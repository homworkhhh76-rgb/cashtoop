# تقرير اختبار Revision 56

- فحص Syntax لجميع ملفات JavaScript بواسطة `node --check`: ناجح.
- فحص جميع كتل JavaScript المضمنة داخل ملفات HTML: ناجح (35 كتلة، بدون أخطاء Syntax).
- التحقق من وجود معادلة إجمالي الصنف `السعر × الكمية` داخل سلة الكاشير: ناجح.
- التحقق من نافذة معلومات التكلفة والربح/الخسارة وزرها بجانب الحذف: ناجح.
- التحقق من حساب تكلفة الوحدة المختارة باستخدام معامل الوحدة: ناجح.
- التحقق من وسم المنتجات المصنعة «غير متعدد» وإجبار `hasVariants=false` عند حفظ وصفة التصنيع: ناجح.
- التحقق من احتساب المخزون الافتراضي للمنتج المركب من مكوناته في صفحة المنتجات والكاشير: ناجح.
- التحقق من رفع طبقة القائمة الجانبية فوق شريط التنقل السفلي: ناجح.
- التحقق من اكتمال 63 ملفاً محلياً في قائمة App Shell وعدم وجود ملفات مفقودة: ناجح.
- تحديث الكاش إلى `v66-r56-item-total-insights-composite-stock-sidebar-cache-first`: ناجح.

# تقرير فحص Revision 51 — العملات المتقاطعة / الوحدات / الأصناف / Cache First (2026-07-22)

## الفحوص المنفذة
- نجح `node --check` لجميع ملفات JavaScript المستقلة في جذر المشروع.
- نجح استخراج وفحص جميع كتل JavaScript المضمنة في 38 صفحة HTML دون أخطاء صياغة.
- تم التحقق من قائمة `LOCAL_ASSETS` في Service Worker: 60 ملفاً محلياً، ولا يوجد ملف مفقود.
- تم التحقق من عدم وجود أي مرجع قديم إلى `multi-system.js?v=50` داخل صفحات HTML.
- تم اختبار منطق العملات مباشرة: 10 دولار = 30 شيكل = 6 دنانير عند 1 دولار = 3 شيكل و1 دينار = 5 شيكل.
- تم اختبار تسلسل الوحدات مباشرة: قطعة 1، كرتونة 24، مشطاح 576.
- تم فحص أن `materials.html` لا تُحمَّل بياناتها داخل قائمة منتجات الكاشير أو قائمة المنتجات؛ الاستهلاك يتم من `cashtop_materials` داخل محرك التصنيع.
- تم التحقق من إضافة `cashtop_materials` و`cashtop_material_purchases` إلى مفاتيح البيانات والمزامنة والنسخ الاحتياطي.
- تم التحقق من أن الحقول `required` المتبقية في إعدادات النظام تخص إجراءات مستقلة فقط: رابط الشعار وتغيير كلمة المرور.
- تمت إضافة مزامنة فورية بعد حفظ إعدادات الإشعارات والضريبة والطابعة والباركود والتخزين، إضافة إلى مزامنة الإعدادات الرئيسية الموجودة.

## حدود الفحص
- لم يتم الاتصال الفعلي بقاعدة بيانات المستخدم أو تشغيل مزامنة حية بين جهازين من بيئة الفحص.
- لم يتم تنفيذ طباعة PDF/Excel داخل متصفح فعلي في هذه البيئة؛ تم فحص مسارات الكود والمكتبات والمراجع محلياً.
- يوصى بعد النشر باختبار عملية بعملة مختلفة عن عملة الصندوق على جهازين، ثم التحقق من الرصيد والحركة على الجهاز الآخر، واختبار تصنيع منتج من صنف خام ذي أكثر من وحدة.

# تقرير فحص Revision 50 — Multi Units / Multi Currency / Cache First (2026-07-22)

- فحص صياغة ملفات JavaScript المستقلة وجميع كتل JavaScript المضمنة في صفحات HTML بواسطة `node --check`: ناجح دون أخطاء صياغة.
- التحقق من عدم وجود معرفات HTML مكررة `id` داخل صفحات التطبيق: ناجح.
- اختبار تحويل الوحدات المتسلسلة: قطعة × 24 = كرتونة، كرتونة × 24 = مشطاح؛ المعاملات التراكمية الناتجة `[1, 24, 576]`: ناجح.
- اختبار سعر صرف بصيغة 100 من العملة الأساسية = 27 من العملة الإضافية؛ تحويل 27 إلى الأساس أعاد 100، وتحويل 100 من الأساس أعاد 27: ناجح.
- التحقق من تحميل `multi-system.js` في جميع الصفحات الأساسية التي تحمل `cashtop-core.js`: ناجح.
- التحقق من أن شاشة الوحدات لا تحتوي حقول رمز/كود الوحدة القديمة: ناجح.
- التحقق من أن Service Worker يستخدم Cache First للملفات المحلية وأن `multi-system.js` ضمن `LOCAL_ASSETS`: ناجح.
- لم يتم تنفيذ اختبار حي على قاعدة بيانات المستخدم الفعلية أو اختبار شامل داخل متصفح/جهاز فعلي؛ التحقق الحالي يغطي سلامة الكود والبنية والمنطق الحسابي الثابت، ويجب اختبار دورة بيع/شراء ومزامنة فعلية بعد النشر.

# تقرير فحص Revision 37 — Printing / Android Blank Preview / Cache First (2026-07-18)

- فحص صياغة جميع ملفات JavaScript المستقلة بواسطة `node --check`: ناجح.
- استخراج وفحص 29 كتلة JavaScript مضمنة داخل صفحات HTML مع استثناء JSON: ناجح.
- التحقق من وجود جميع الملفات المحلية الـ54 المسجلة في `LOCAL_ASSETS` داخل Service Worker: ناجح.
- التحقق الثابت من أن طباعة النظام تستخدم مستند iframe مستقل وتطبع HTML الفاتورة مباشرة بدلاً من إدراج صورة Canvas في الصفحة الأصلية: موجود.
- التحقق من انتظار الخطوط والصور قبل استدعاء `print()` ومن إنشاء CSS منفصل لمقاسات 58mm و80mm وA4: موجود.
- التحقق من إضافة مسار WebUSB للطباعة الحرارية المباشرة مع إبقاء Bluetooth BLE وBluetooth/Serial: موجود.
- التحقق من تحديث روابط وحدة الطباعة إلى `cashtop-printer.js?v=37` وتحديث Service Worker إلى `v47-r37-print-iframe-cache-first`: موجود.
- لم يتم تنفيذ اختبار فعلي داخل Android Print Spooler أو على طابعة حرارية مادية من بيئة الفحص؛ يلزم اختبار جهاز حقيقي للتأكد من تعريف الطابعة والمقاس المدعوم من نظام التشغيل.

# تقرير فحص Revision 34 — Realtime Sync / IndexedDB / Backup Import (2026-07-18)

- فحص صياغة جميع ملفات JavaScript المستقلة البالغ عددها `15` ملفاً بواسطة `node --check`: ناجح.
- استخراج وفحص جميع كتل JavaScript المضمنة في صفحات HTML مع استثناء كتل JSON: `29` كتلة، ناجح دون أخطاء صياغة.
- التحقق من أن جميع الملفات المحلية الـ`54` المسجلة في `LOCAL_ASSETS` داخل Service Worker موجودة فعلياً: ناجح.
- التحقق الثابت من وجود طبقة IndexedDB المتينة، ونسخ بيانات الشركة والـ metadata وطابور المزامنة إليها، ومسار fallback عند `QuotaExceededError`: موجود.
- التحقق الثابت من ترحيل طوابير المزامنة القديمة المرتبطة بنفس tenant ومن نقل بيانات Dataset المعلقة من مساحة المفتاح القديمة عند الحاجة: موجود.
- التحقق من أن الاستيراد الكامل واستيراد الأقسام يضيفان Datasets إلى المزامنة القسرية `forceReplace` ويستدعيان المزامنة السحابية مع إعادة المحاولة: موجود.
- التحقق من حماية `cashtop_company_access` أثناء الاستيراد من إعادة حالة أو مفتاح أو مدير قديم: موجود.
- التحقق من وجود بث Firebase عبر EventSource مع Polling احتياطي، ومن تسلسل `syncAll` لمنع تعارض دورات المزامنة: موجود.
- التحقق من إضافة شريط تقدم المزامنة ومؤشر تحميل السجلات أثناء Pull: موجود.
- لم يتم تنفيذ اختبار حي بين جهازين على قاعدة Firebase/MongoDB الفعلية، ولم يتم اختبار انقطاع الشبكة الفعلي أو حدود تخزين المتصفح على جهاز المستخدم؛ يلزم اختبار نشر واقعي للتحقق من قواعد قاعدة البيانات ودعم EventSource في بيئة الاستضافة.

# تقرير فحص Revision 31 — Performance/Firebase Patch (2026-07-16)

- فحص صياغة جميع ملفات JavaScript المستقلة (`14` ملفاً) بواسطة `node --check`: ناجح.
- استخراج وفحص جميع كتل JavaScript المضمنة في صفحات HTML مع استثناء كتل JSON: ناجح دون أخطاء صياغة.
- تحليل ملفات قواعد Firebase و`manifest.webmanifest`: ناجح.
- التحقق من أن جميع الملفات الـ`53` المسجلة في `LOCAL_ASSETS` داخل Service Worker موجودة فعلياً، بما فيها `cashtop-worker.js`: ناجح.
- اختبار مباشر لمهام Worker: البحث في السجلات، الفرز حسب التاريخ، وتجميع إحصائيات الفواتير: ناجح.
- التحقق الثابت من وجود مسار fallback للمفتاح عبر `cashTopAdmin/companies` ومن أن فشل `keyIndex` لا يوقف المحاولة الاحتياطية: ناجح.
- التحقق من أن Bootstrap الدخول لا يجلب Dataset المنتجات أو الفواتير، ويقتصر على بيانات الوصول الأساسية: ناجح.
- التحقق من وجود Progressive Sync وBackground Pull حسب الصفحة، ومن أن القراءة الدورية تستخدم مجموعات الصفحة بدلاً من `reconcileAll()` الكامل: ناجح.
- التحقق من Cache-First للملفات المحلية مع تحديث صامت في الخلفية حتى عند توفر الإنترنت: ناجح.
- التحقق من وجود معاملات ذرية للحفظ والحذف في فواتير البيع والمشتريات ومرتجعات المشتريات، مع استعادة WAL عند بدء التطبيق: ناجح.
- التحقق من إضافة فهارس Firebase المطلوبة في `database.rules.json` و`database.rules.compatibility.json` و`firebase-rules.json` وملف الدمج: ناجح.
- لم يتم تنفيذ اختبار Firebase حي على مشروع المستخدم من بيئة الفحص؛ يلزم بعد النشر التأكد من نشر قواعد Realtime Database المحدثة واختبار دخول مفتاح كان `keyIndex` الخاص به مفقوداً أو قديماً.

# Revision 31 - فحوص إنشاء مفاتيح الشركات

- تم إعادة إنتاج سبب الخطأ باستخدام `cashtop_admin_licenses` و`cashtop_admin_users` بصيغة Object قديمة بدلاً من Array.
- نجح اختبار إنشاء شركة ومفتاح جديد فعلياً داخل بيئة JavaScript مع البيانات القديمة؛ تم تحويل القوائم إلى Array، والإبقاء على المفتاح القديم، وإضافة المفتاح الجديد ومدير الشركة وحفظ `tenantId` مستقل.
- تم اختبار صيغ Array وObject والسجل المفرد وJSON مزدوج الترميز وFirebase payload، وجميعها أصبحت قابلة لاستخدام `filter/find` بدون خطأ.
- نجح فحص Syntax لجميع ملفات JavaScript المستقلة البالغ عددها 13 ملفاً.
- نجح فحص 29 سكربت JavaScript مضمناً داخل صفحات HTML.
- تم تحديث Service Worker إلى `v38-admin-key-create-fix-cache`.

# Revision 30 - فحوص دفعات العملاء والدين اليدوي

- تم التحقق من وجود زري **دفعة** و**دين يدوي** لكل عميل داخل عمود الإجراءات.
- تم التحقق من أن تسجيل الدفعة ينشئ سند قبض `relationType=client` ويضيف المبلغ إلى الصندوق المختار ويخصمه من رصيد العميل.
- تم التحقق من أن حركة الصندوق تحمل `sourceType=voucher` و`sourceId` المطابق للسند، لتبقى قابلة للعكس من صفحة السندات.
- تم التحقق من أن الدين اليدوي يضاف داخل `debtInvoices` للعميل ويزيد رصيده دون تعديل الصناديق.
- تم التحقق من أن سجل العميل يعرض الدين اليدوي بمرجعه وملاحظاته، وأن الدفعة تظهر كسداد/سند قبض.
- تستخدم العمليات مفاتيح التخزين المدارة `cashtop_customers` و`cashtop_funds_db` و`cashtop_vouchers`، ولذلك تدخل في طابور مزامنة Firebase المعزول للشركة والفرع.
- تم تحديث Service Worker إلى `v37-customer-payment-debt-sync-cache`.

# Revision 29 - فحوص إصلاح باركود iPhone

- تم التحقق أن `barcode-tools.js` ينشئ `Html5Qrcode` بإعداد `useBarCodeDetectorIfSupported: !IS_IOS`، وبالتالي يستخدم iPhone/iPad محرك ZXing بدلاً من BarcodeDetector الأصلي.
- تم التحقق من تطبيق نفس الإعداد على البث المباشر وقراءة ملفات الصور.
- تم التحقق من تحديث روابط `barcode-tools.js?v=20` في الكاشير والمنتجات والمشتريات.
- تم التحقق من تحديث Service Worker إلى `v36-ios-barcode-decode-fix-cache`.
- تم إجراء فحص Syntax لملفات JavaScript وفحص سلامة حزمة ZIP.
- لا تتوفر في بيئة الاختبار الحالية كاميرا iPhone فعلية، لذلك اختبار القراءة النهائي على عدسة iPhone يبقى اختبار جهاز حقيقي بعد النشر.

# تقرير اختبار Revision 28

- نجح فحص صياغة 13 ملف JavaScript مستقلاً بواسطة `node --check` دون أخطاء.
- نجح فحص 29 سكربت JavaScript مضمناً داخل صفحات HTML دون أخطاء صياغة.
- نجح تحليل 7 ملفات JSON/Manifest.
- تم التحقق من وجود جميع صفحات HTML البالغ عددها 34 صفحة داخل قائمة كاش Service Worker دون صفحات ناقصة.
- تم التحقق من تثبيت رابط `html5-qrcode@2.3.8` نفسه في الكاش وصفحات الكاشير والمنتجات والمشتريات.
- تم التحقق من أن تشغيل الماسح لا يستدعي `requestPermission()` قبل فتح محرك المسح، لمنع فتح/إغلاق الكاميرا مرتين على iPhone.
- تم التحقق من إضافة `playsinline` و`webkit-playsinline` إلى فيديو المسح المباشر.
- تم التحقق من وجود مسار احتياطي لالتقاط صورة بالكاميرا عبر `capture=environment` عند فشل البث المباشر.
- تم التحقق من إضافة `Permissions-Policy: camera=(self), microphone=()` إلى إعدادات Firebase Hosting.
- تم التحقق من سياسة Cache First للملفات المحلية مع تحديث صامت في الخلفية، ومن توحيد مفتاح الكاش بإزالة معاملات الاستعلام قبل `cache.put` لمنع بقاء نسخة قديمة موازية.
- تم التحقق من أن بيانات Firebase وواجهات المصادقة الحية لا تدخل Cache Storage.
- لم يتم تنفيذ اختبار مادي على جهاز iPhone حقيقي داخل بيئة الفحص؛ لذلك يبقى اختبار طلب الإذن والبث الفعلي على Safari/iOS بعد النشر عبر HTTPS خطوة تشغيلية مطلوبة.

# تقرير اختبار Revision 27

- نجح فحص صياغة جميع ملفات JavaScript المستقلة بواسطة `node --check`.
- نجح فحص 29 سكربت JavaScript مضمناً داخل صفحات HTML دون أخطاء صياغة.
- نجح اختبار منطق النقل المباشر: خصم 120 قطعة من «مخزن محمد» في الفرع الرئيسي وإضافتها إلى «مخزن محمود» في فرع 2 مع بقاء الأرصدة صحيحة.
- نجح اختبار النقل إلى المحل الرئيسي لفرع فرعي وتحديث `branchStocks` الخاص بذلك الفرع.
- نجح اختبار منتج متعدد المقاسات: نقل كمية مقاس محدد من مخزن فرع إلى المحل الرئيسي لفرع آخر وتحديث رصيد المقاس والرصيد الإجمالي للفرع.
- تم التحقق من ربط كل مخزن بفرعه بواسطة `branchId` ومن أن قائمة مواقع النقل تعرض المحل الرئيسي والمخازن التابعة للفرع المختار فقط.
- تم التحقق من أن الموظف/مدير الفرع غير العام لا يستطيع تغيير الفرع المصدر، بينما حساب مدير الشركة يستطيع اختيار أي فرع.
- تم التحقق من أن صلاحية `inventory.transfer` تسمح بدخول صفحة الفروع لغرض النقل دون منح صلاحية إدارة الفروع.
- تم التحقق من أن نقل البيانات العابرة للفروع يكتب مجموعة المنتجات الكاملة مباشرة في مساحة المستأجر نفسه ويضيف عملية مزامنة دون إسقاط تعديلات الفروع الأخرى.
- تم التحقق من عزل `storeStocks` في القراءة والدمج لكل فرع لمنع استبدال مخزون فرع آخر عند حفظ منتج من جلسة فرع محلية.

# تقرير اختبار Revision 26

- نجح فحص صياغة جميع ملفات JavaScript عبر `node --check`.
- نجح فحص 29 سكربت JavaScript مضمناً داخل صفحات HTML.
- نجح فحص 1261 مرجعاً محلياً داخل صفحات HTML دون ملفات مفقودة.
- تحتوي قائمة Service Worker على 52 أصلاً محلياً وجميعها موجودة.
- نجح تحليل جميع ملفات JSON الستة.
- تم التحقق ثابتاً من أن مساحة التخزين المحلية ومسار Firebase وطابور المزامنة تعتمد `tenantId` الثابت.
- تم التحقق من وجود حارس هوية يمنع قبول عقدة Firebase ذات `tenantId` أو `companyKey` مختلف.
- تم التحقق من إلغاء المسارات البديلة ذات معرّف شركة مختلف، ومنع الاعتماد على المفتاح وحده في اختيار مسار البيانات.
- تم التحقق من تثبيت جلسة الشركة لكل تبويب عبر `sessionStorage` لمنع تبديل شركة التبويب عند تسجيل الدخول إلى شركة أخرى في تبويب ثانٍ.
- تم التحقق من حماية ترحيل مفاتيح `localStorage` القديمة غير المعزولة بحيث لا تنتقل تلقائياً إلى مستأجر جديد عند وجود مساحة شركة أخرى.
- لم يتم الاتصال بمشروع Firebase الحقيقي أثناء الفحص، لذلك يلزم نشر قواعد Realtime Database المرفقة مع النسخة عند اعتمادها على المشروع الفعلي.

# تقرير اختبار Revision 18

- نجح فحص صياغة 12 ملف JavaScript و29 سكربتاً مضمناً داخل صفحات HTML.
- نجح فحص 1244 مرجعاً محلياً دون ملفات مفقودة.
- تحتوي قائمة Service Worker على جميع صفحات HTML البالغ عددها 31 صفحة، إضافة إلى ملفات التطبيق المحلية اللازمة.
- نجح اختبار Service Worker: صفحات التطبيق المحلية تُعالج بسياسة Cache First، بينما طلبات `firebaseio.com` لا يتم اعتراضها أو تخزينها.
- نجح اختبار مزامنة معزول لشركتين `CMP_A` و`CMP_B`: رفع بيانات كل مفتاح إلى مساره فقط، ثم سحب بيانات الشركة الثانية على جهاز افتراضي آخر دون اختلاط.
- نجح اختبار بقاء `backupImportEnabled` المرجعي عند وجود تعديل محلي معلّق، مع استمرار رفع تعديل مدير الشركة وعدم توقف المزامنة.
- تم التحقق من أن لوحة المشرف تستخدم كتابة مستقلة لمجموعة `cashtop_company_access` وبيانات `meta` بدلاً من PUT لعقدة الشركة كاملة.
- لم يتم الاتصال بمشروع Firebase الحقيقي من بيئة الفحص؛ اختبارات الشبكة نُفذت بخادم Realtime Database وهمي يحاكي GET وPUT وETag ومسارات شركتين.

# تقرير اختبار Revision 17

- نجح فحص صياغة جميع ملفات JavaScript و29 سكربتاً مضمناً داخل صفحات HTML.
- نجح اختبار خصم كرتونة تحتوي 10 قطع من رصيد 100، فأصبح الرصيد 90، ثم خصم قطعتين فأصبح 88.
- نجح اختبار الفرع الفرعي: تحديث الرصيد المسقط للفرع و`branchStocks` معاً، بما في ذلك الأصناف ذات المقاسات.
- نجح اختبار إزالة القيمة القديمة الخاطئة `branchStocks.MAIN` وعدم استخدامها لعرض رصيد الفرع الرئيسي.
- نجح اختبار أن زر الحفظ العادي لا يستدعي الطباعة، حتى لو بقي خيار `autoPrint` قديماً في الإعدادات.
- نجح فحص سياسة Service Worker: الصفحات والأصول المحلية تُرجع من الكاش مباشرة عند توفرها.
- تم فحص 251 مرجعاً محلياً داخل HTML و52 أصلاً محلياً في قائمة الكاش دون ملفات مفقودة.
- تعذر تشغيل اختبار Chromium الكامل في بيئة الفحص لأن المتصفح حظر عناوين الخادم المحلي بسياسة إدارية؛ لذلك استُخدم اختبار منطق JavaScript مباشر مع فحوص الصياغة والمراجع.

# تقرير فحص كاش توب 2 — Revision 16

## الفحوص المنفذة

- فحص صياغة 12 ملف JavaScript بواسطة `node --check` دون أخطاء.
- استخراج وفحص 29 سكربتاً مضمناً داخل صفحات HTML بواسطة `node --check` دون أخطاء.
- تحليل 1244 مرجعاً محلياً للصفحات والسكربتات والصور والأنماط، ولم توجد ملفات محلية مفقودة.
- التحقق من صلاحية 6 ملفات JSON قابلة للتحليل.
- تشغيل اختبار متصفح معزول بواسطة Playwright/Chromium بعرض جوال 390 بكسل. النتيجة `PASS`.
- إنشاء معاينة مرئية للجوال والتحقق من أن عرض الصفحة يساوي عرض الشاشة `390/390` دون تمرير أفقي.

## سيناريو اختبار المتصفح

نفذ الاختبار دورة كاملة داخل نافذة ترحيل المبيعات:

1. كتب حرفاً من اسم عميل مسجل وتحقق من ظهور اقتراح فعلي.
2. اختار العميل وتحقق من تعبئة رقم الجوال تلقائياً.
3. كتب حرفاً من اسم منتج واختاره من الاقتراحات.
4. أضاف صنفاً حراً غير موجود في المخزون.
5. أدخل الكميات والأسعار والمدفوع، واختار الصندوق الثاني.
6. أغلق النافذة وتحقق من حفظ المسودة في التخزين المحلي.
7. أزال محتوى النافذة من DOM وأعاد فتحها، ثم تحقق من استعادة العميل والصنفين والصندوق.
8. رحّل الفاتورة وتحقق من النتائج التالية:
   - فاتورة واحدة بإجمالي `23` ومدفوع `5` ودين `18`.
   - إيداع `5` في الصندوق المختار فقط، وارتفاع رصيده من `50` إلى `55`.
   - إضافة دين `18` إلى رصيد العميل وتسجيله ضمن `debtInvoices` برقم الفاتورة.
   - انخفاض مخزون المنتج المسجل من `5` إلى `3`.
   - حفظ الصنف غير الموجود كسطر مخصص `isCustom=true`.
   - حذف مسودة الترحيل بعد نجاح الحفظ.

## نقاط التحقق الثابتة

- حقلا العميل والصنف لا يستخدمان `datalist` أو سهم قائمة.
- الاقتراحات تظهر من أول حرف وتدعم البحث بالاسم، ورقم الجوال للعملاء، والباركود للمنتجات.
- كل بطاقة فاتورة تحتوي اختيار صندوق مستقل.
- الترحيل يمنع فقد المبلغ المدفوع إذا لم يوجد صندوق صالح، ويعيد جميع البيانات إلى اللقطة السابقة عند الخطأ.
- حذف الفاتورة يعكس رصيد العميل والصندوق والمخزون، ويحذف تفصيل الدين المرتبط برقم الفاتورة.
- صفحة الفواتير تستخدم مراجع `v16`، وService Worker يستخدم Cache `v24`.

## حدود الفحص

لم يتم الدخول إلى مشروع Firebase الخاص بالمستخدم، لذلك لم تُنفذ مزامنة فعلية بين جهازين. اختبارات التخزين والحسابات والواجهة نُفذت محلياً، بينما تعتمد المزامنة على اعتراض `localStorage` وطابور Firebase الموجودين في المشروع. بعد النشر يُنصح بترحيل فاتورتين من هاتف فعلي، ثم التأكد من الصندوق والعميل وسجل الفواتير على جهاز ثانٍ.

## فحوص Revision 24
- تم فحص صياغة JavaScript لجميع كتل script الداخلية في صفحات HTML عبر Node.js: ناجح.
- تم فحص `cashtop-export.js` عبر `node --check`: ناجح.
- تم التأكد من عدم وجود أي مسار PDF متبقٍ يستخدم `new jsPDF` أو `pdf.addImage` داخل صفحات HTML؛ إنشاء PDF أصبح مركزياً في محرك التصدير الجدولي.
- تم التأكد من ربط كل صفحة تستخدم `CashtopExport` بملف `cashtop-export.js`.
- تم التأكد من بقاء وظائف تصدير الصور منفصلة؛ التغيير يخص PDF ولا يمنع خيار حفظ الصور حيث كان موجوداً.


## فحص تحويل MongoDB API

- تم التحقق من أن إعداد الواجهة لا يحتوي على `mongodb+srv` أو `MONGODB_URI`.
- تم ربط نقطة البيانات بالـ API الوسيط بدل Firebase RTDB المباشر.
- تم استثناء API الحي من Service Worker Cache.
- تم الحفاظ على طبقة Progressive Loading وLazy Rendering وWeb Worker والمعاملات المحلية الخاصة بـ Revision 31.

## فحص Revision 41 - النوافذ / 120Hz-ready / Cache First
- `node --check cashtop-core.js`: ناجح.
- `node --check service-worker.js`: ناجح.
- التحقق من قائمة App Shell: جميع ملفات HTML الموجودة (34/34) مدرجة في الكاش، ولا توجد صفحات HTML مفقودة.
- التحقق من أن API الحي مستثنى من Cache Storage: بقيت قواعد `isLiveApiRequest` فعالة.
- التحقق من Cache First المحلي: `localCacheFirst()` يعيد النسخة المخزنة فوراً، والتحديث الشبكي يتم بالخلفية مع throttling.
- ملاحظة: معدل 120 إطار/ثانية الفعلي يعتمد على شاشة الجهاز، المتصفح، WebView وقدرة المعالج؛ النسخة محسنة للاستفادة من 120Hz عبر requestAnimationFrame وتقليل layout/paint ولا يمكن للويب فرض 120Hz على جهاز لا يدعمه.

## فحص Revision 33 — المزامنة والطباعة وتقارير الفواتير

- نجح `node --check` لجميع ملفات JavaScript الموجودة في جذر المشروع: 15 ملفاً دون أخطاء صياغة.
- نجح استخراج وفحص 29 كتلة JavaScript مضمنة داخل صفحات HTML دون أخطاء صياغة.
- تم فحص 1265 مرجعاً محلياً من `src` و`href` داخل صفحات HTML ولم توجد ملفات محلية مفقودة.
- تم التحقق ثابتاً من أن تعديل المنتج يستخدم كمية `stockPieces` الجديدة وكمية المتغير الجديدة بدلاً من إعادة الكمية السابقة.
- تم التحقق من أن طابور المزامنة يعالج كل dataset داخل حلقة مستقلة مع `try/catch` خاص بها، وأن الفشل لا يرمي خطأ يوقف العناصر التالية، وأن النجاح يستدعي إزالة العملية المعلقة لذلك المفتاح فوراً.
- تم التحقق من حفظ طابور العمليات في التخزين المحلي واستدعاء النسخة الاحتياطية في IndexedDB قبل تسجيل الخروج، ومن وجود إعادة محاولة تلقائية عند عودة الاتصال.
- تم التحقق من وجود فلاتر التقرير اليومية/الأسبوعية/الشهرية/السنوية/المخصصة، وتصدير Excel وPDF بترويسة اسم وشعار المحل.
- تم التحقق من تكامل محرك الطباعة مع الكاشير وسجل الفواتير وإعدادات الطابعة، ودعم 58mm و80mm وA4 والباركود والشعار وBluetooth BLE وBluetooth Classic/Serial عند توفر واجهة المتصفح.
- تم التحقق من إضافة حقل باركود مستقل لكل منتج داخل ترحيل الفواتير المتعددة، مع البحث الفوري بالباركود.
- لم يُجرَ اتصال فعلي بقاعدة MongoDB/Firebase الخاصة بالمستخدم أو بطابعة Bluetooth/USB حقيقية من بيئة الفحص؛ التحقق الحالي يغطي الصياغة، المراجع المحلية، ومسارات المنطق الثابتة. يلزم اختبار جهازين وطابعة فعلية بعد النشر للتأكد من صلاحيات المتصفح وتعريفات الطابعة والشبكة الحقيقية.


## اختبار Revision 52

- تم فحص صياغة JavaScript للملفات المعدلة: `manufacturing.js` و`service-worker.js` و`cashtop-core.js`.
- تم فحص JavaScript المضمن داخل `products.html` و`materials.html` و`المشتريات.html` و`ادارة التصنيع.html`.
- تم التأكد أن جميع ملفات `LOCAL_ASSETS` المسجلة في Service Worker موجودة فعلياً داخل الحزمة.
- تم التأكد من أن قوائم طريقة الإضافة في المنتجات والأصناف والمشتريات تستخدم اسم أكبر وحدة فقط.
- تم التأكد من بقاء حقلي باركود/سعر الوحدة جنباً إلى جنب على شاشات الجوال في إضافة المنتجات.
- تم التأكد من ترتيب حقليّن جنباً إلى جنب في نموذج إضافة الأصناف على الجوال.
- تم التأكد من أن اقتراحات مكونات التصنيع تعرض مجموعة `الأصناف ومواد التصنيع` قبل مجموعة المنتجات.
- تم تحديث إصدار الكاش إلى `v62-r52-largest-unit-mobile-pairs-materials-120hz-cache-first`.
- لا يمكن ضمان 120FPS رقمياً من اختبار ملفات ثابتة؛ المعدل الفعلي يعتمد على الجهاز والمتصفح وحجم البيانات.

## تقرير فحص Revision 53
- نجاح `node --check` لجميع ملفات JavaScript المحلية.
- نجاح فحص Syntax لجميع كتل JavaScript المضمنة في صفحات HTML.
- التحقق من وجود جميع ملفات `LOCAL_ASSETS` المشار إليها في Service Worker: لا توجد ملفات مفقودة.
- التحقق من تحديث إصدار الكاش إلى Revision 53 وإضافة `audit-trail.html`.
- التحقق من `step=any` لكميات التصنيع وتخطيط حقلين على الجوال.
- التحقق من وجود نظام مسودات النوافذ، سجل النشاط IndexedDB، النوافذ الفورية، تحديث الصناديق، وخيار تقريب الطباعة.
- التحقق من وجود أنماط أجور: شهري/أسبوعي/يومي/بالساعة/فوري.
- لم يتم اختبار قاعدة بيانات الإنتاج أو مزامنة عدة أجهزة حقيقية من هذه البيئة.


## Revision 54 — اختبارات سجل النشاط والإشعارات

- تم فحص Syntax لجميع ملفات JavaScript وملفات API النموذجية بواسطة `node --check` بدون أخطاء.
- تم استخراج وفحص JavaScript المضمن داخل جميع صفحات HTML بدون أخطاء Syntax.
- تم التأكد أن جميع ملفات `LOCAL_ASSETS` في Service Worker موجودة فعلياً.
- تم التأكد من حذف `maintenance.html` وعدم وجود مراجع تشغيلية لوضع الصيانة.
- تم التأكد من إزالة مراجع نظام `cashtop_broadcast_popups` والنوافذ الفورية من ملفات التشغيل.
- تم التأكد من وجود مسار سجل النشاط الجديد `auditTrailRecent` وحد أقصى 100 عملية مع حذف الأقدم.
- تم التأكد من وجود دعم Service Worker لأحداث `push` و`notificationclick` و`periodicsync` ورسائل عرض الإشعار المحلية.
- تم التأكد من إضافة صورة `notification-icon.png` إلى كاش التطبيق.
- تم تحديث الكاش إلى `v64-r54-audit100-notifications-no-maintenance-cache-first`.
- إشعارات Web Push العامة تحتاج نشر خدمة Push وضبط VAPID كما هو موضح في `PUSH-NOTIFICATIONS-SETUP-AR.md`؛ لا يمكن اختبار الإرسال الحقيقي إلى أجهزة خارجية من بيئة التطوير الحالية.


## Revision 55 — اختبارات التحقق

- نجاح فحص Syntax لجميع ملفات JavaScript الأساسية وملفات Push API.
- نجاح فحص جميع JavaScript المضمن داخل صفحات HTML بدون أخطاء Syntax.
- تم التحقق من حذف `audit-trail.html` وعدم وجود روابط متبقية له في صفحات أو سكربتات التطبيق.
- تم التحقق من أن جميع ملفات `LOCAL_ASSETS` في Service Worker موجودة فعلياً (63 ملفاً).
- تم تحديث الكاش إلى `v65-r55-transient-manager-bom-cache-first`.
- تم التحقق من وجود قناة الإشعارات العابرة وإرسال إشعار الفاتورة إلى قناة الشركة الخاصة بالمدير.
- تم التحقق من حفظ `cashtop_materials` مع عملية البيع للمنتجات المركبة وربط خصم المكونات عند البيع.
- ملاحظة: اختبار التسليم الحقيقي بين أجهزة متعددة يحتاج تشغيل النسخة على الاستضافة الفعلية المتصلة بخدمة MongoDB HTTPS.
