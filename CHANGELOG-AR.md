# Revision 48 - إصلاح المقاسات والنوافذ والشريط السفلي والفاتورة الحرارية

- إصلاح المساحات البيضاء وتمدد المقاسات على اللابتوب بإلغاء احتواء التخطيط المتسبب في كسر العناصر الثابتة، مع عدم تغيير تصميم الجوال.
- إعادة النوافذ المنبثقة إلى وسط الشاشة وبالمقاسات المدمجة الأصلية بدل التمدد الكبير.
- تثبيت شريط إجماليات وأزرار الكاشير على الحافة السفلية الحقيقية للشاشة في وضع سطح المكتب.
- تصغير الفاتورة الحرارية وتقليل الفراغ العلوي والخطوط والمسافات لتوفير الورق مع الإبقاء على الرندر عالي الدقة.
- إزالة عنوان «فاتورة بيع ضريبية» واسم الفرع والكاشير وطريقة الدفع وعدد الأصناف المباعة من التصميم الحراري الافتراضي.
- جعل اسم المنتج سطراً واحداً داخل جدول الفاتورة.
- تحديث كاش التطبيق إلى Revision 48.

## نسخة Portable Hosting - بدون rtdb.js

- حذف `api/rtdb.js` من الحزمة.
- إلغاء الاعتماد على `/api/rtdb` داخل نفس نطاق الاستضافة.
- جعل الواجهة تستخدم MongoDB HTTPS Gateway خارجي ثابت، لذلك يمكن رفع ملفات الواجهة على أي استضافة ثابتة.
- إزالة fallback إلى same-origin API من المزامنة وتسجيل الدخول.
- إزالة MongoDB server dependency من `package.json`.


## Revision 38
- إصلاح تجمّد تمرير الصفحة عند السحب من داخل الجداول بعد وصول التمرير الداخلي إلى الأعلى أو الأسفل.
- السماح بانتقال السحب العمودي من الجدول إلى الصفحة مع الإبقاء على التمرير الأفقي للجداول.
- تحديث إصدار الكاش إلى v48-r38-table-scroll-chain-cache-first.
# Revision 37
- إصلاح معاينة الطباعة البيضاء على Android باستبدال طباعة صورة Canvas بمستند طباعة HTML مستقل داخل iframe.
- تطبيق تصميم الفاتورة الحرارية الجديد مركزياً على 58mm و80mm وA4 للكاشير وسجل الفواتير.
- انتظار الخطوط والصور قبل الطباعة وإضافة WebUSB واختبار طباعة تجريبية من إعدادات الطابعة.
- تحديث كاش التطبيق إلى v47 مع استمرار Cache First الفوري للصفحات حتى مع توفر الإنترنت.

# Revision 36
- إصلاح فشل تحميل firebase-sync.js عند فتح التطبيق من file:// على Android بتحويله من Module Script إلى Classic Script.
- إضافة محمل ذاتي لوحدة المزامنة يصلح الصفحات القديمة المخزنة في الكاش.
- منع مسار null/api/rtdb غير الصالح في وضع file://.
- رفع كاش التطبيق إلى v46 مع استمرار Cache First للصفحات حتى أثناء توفر الإنترنت.

# Revision 35 — إصلاح الاتصال الوهمي وتحديث Cache First

- إزالة الاعتماد الصارم على `navigator.onLine` في المزامنة والدخول.
- إزالة رؤوس CORS غير الضرورية من طلبات MongoDB API التي كانت قد تسبب `Failed to fetch`.
- إضافة إعادة محاولة ذكية ومسار API احتياطي على نفس النطاق عند توفره.
- تحديث Service Worker إلى كاش جديد وتحميل الحزمة كاملة Cache First مع تحديث خلفي.
- تحديث لوحة الإدارة لتزامن البيانات دون بوابة `navigator.onLine`.

# Revision 34 — مزامنة لحظية وIndexedDB واستيراد متين (2026-07-18)

- ترحيل طوابير المزامنة القديمة المرتبطة بنفس `tenantId` إلى الطابور الحالي، مع نقل قيمة Dataset المعلقة من مساحة المفتاح القديمة عندما تكون أحدث، ثم مزامنتها بدلاً من تركها معلقة.
- إضافة طبقة تخزين محلية متينة `IndexedDB` باسم `cashtop-local-durable-v2` لكل بيانات الشركة والـ metadata والطابور والمعاملات، مع إبقاء `localStorage` كـ hot cache متزامن، والتحول إلى الذاكرة + IndexedDB عند امتلاء حصة localStorage بدلاً من فشل الحفظ.
- استعادة بيانات IndexedDB عند الدخول قبل المزامنة السحابية، واستبدال قيم seed الفارغة بالنسخ المحلية المتينة إذا كانت أحدث.
- جعل استيراد النسخة الاحتياطية يضع كل Dataset مستورد في طابور `forceReplace` ويرفعه إلى قاعدة البيانات، مع إعادة المحاولة الفورية وعدم الاكتفاء بحفظه محلياً.
- السماح باستيراد نسخة تحمل مفتاح شركة قديم عندما يكون `tenantId` مطابقاً للشركة الحالية، مع حماية `cashtop_company_access` من استرجاع مفتاح/حالة/مدير قديم قد يؤدي إلى تعطيل الحساب.
- تحديث استيراد نسخ الأقسام لرفع الأقسام المستوردة صراحةً إلى قاعدة البيانات وإظهار حالة العمليات المتبقية.
- إضافة بث Firebase Realtime Database عبر `EventSource/SSE` لتلقي تغييرات الأجهزة الأخرى فوراً تقريباً، مع Polling احتياطي سريع عند عدم توفر البث أو عند استخدام Mongo/API.
- تسريع جدولة المزامنة بعد الحفظ وعودة الإنترنت وفتح الصفحة، ومسح backoff عند عودة الاتصال حتى تبدأ العمليات المعلقة فوراً.
- تسلسل جميع دورات `syncAll` لمنع تصادم الاستيراد أو الحفظ السريع مع دورة مزامنة قائمة وإرجاع `busy`.
- إضافة شريط تقدم صغير أسفل أيقونة المزامنة ومؤشر تحميل دائري صغير داخل السجلات الظاهرة أثناء سحب البيانات من قاعدة البيانات.
- رفع توازي سحب Datasets إلى 6–8 طلبات للدفعات المناسبة، مع إبقاء فشل Dataset منفرد غير مانع لبقية العمليات.
- تحديث Service Worker إلى `v44-indexeddb-realtime-sync-import-progress` مع استمرار Cache-First الفوري للصفحات والملفات المحلية حتى مع توفر الإنترنت.

# Revision 32 — إصلاح المزامنة الدائمة وإعدادات المتجر وMongoDB (2026-07-16)

- تأكيد تشغيل طبقة البيانات عبر `MongoDB Atlas API` المعرفة في `firebase-config.js` مع إبقاء أسماء ملفات التوافق القديمة فقط.
- تحويل مزامنة MongoDB عند وجود تعديلات معلقة من كتابة عقدة الشركة كاملة إلى قراءة/كتابة كل Dataset بشكل مستقل، لتقليل تعارض الأجهزة ومنع قسم من الكتابة فوق قسم آخر.
- إضافة دمج للسجلات المعدلة والمضافة والمحذوفة في المصفوفات، ودمج الحقول المتغيرة في كائنات الإعدادات عند تزامن أكثر من جهاز، مع التحقق بعد الكتابة وإعادة المحاولة عند اكتشاف تعارض.
- حفظ طابور العمليات المعلقة في `localStorage` مع نسخة احتياطية دائمة في `IndexedDB` واستعادته تلقائياً بعد إعادة فتح الجهاز، ثم بدء المزامنة فور عودة الاتصال.
- حماية أي تعديل جديد يحدث أثناء مزامنة جارية من أن يُستبدل بنسخة أقدم وصلت من الشبكة.
- تطبيق اسم المحل والشعار والعنوان ورقم الجوال من إعدادات النظام على هوية الواجهة كاملة، وتحديثها مباشرة بعد الحفظ أو بعد وصول إعدادات أحدث من MongoDB.
- تحديث التقارير PDF/Excel والمشاركة لاستخدام بيانات المحل والشعار من الإعدادات بدلاً من الاسم الثابت.
- تفعيل حساب سعر البيع تلقائياً من سعر الشراء حسب `profitRate` في إضافة المنتجات وفواتير المشتريات، للوحدات والقطع.
- تحديث Service Worker إلى `v42-mongodb-durable-sync-cache-first` مع فتح الصفحات المحلية من الكاش أولاً حتى عند توفر الإنترنت وتحديث النسخة في الخلفية.

# Revision 31 — حزمة تحسين الأداء وثبات Firebase (2026-07-16)

- إصلاح التحقق من مفتاح الشركة عندما يكون مسجلاً في `cashTopAdmin/companies` لكن `keyIndex` مفقوداً أو قديماً أو يفشل طلبه مؤقتاً؛ أصبح الدخول يجرب الفهرس السريع ثم سجل الشركات مع إعادة التحقق من `tenantId` والمفتاح قبل فتح البيانات.
- منع تسجيل الدخول من تنزيل عقدة الشركة كاملة؛ أصبح Bootstrap الدخول يقرأ فقط `meta` وبيانات الوصول والفروع والموظفين، ثم تفتح الواجهة مباشرة.
- تحويل مزامنة القراءة العادية إلى Progressive Data Loading حسب الصفحة، مع تحميل بقية مجموعات البيانات على دفعات في الخلفية أثناء وقت الخمول، وإيقاف الاستعلام الكامل الدوري كل 4 ثوانٍ.
- تحديث Service Worker إلى `v39-progressive-data-cache-first` مع Cache-First حقيقي للصفحات المحلية حتى أثناء توفر الإنترنت، وتحديث صامت في الخلفية.
- إضافة `cashtop-worker.js` لنقل البحث والفرز وإحصائيات الفواتير الثقيلة خارج Main Thread عند كبر البيانات.
- إضافة Debounce موحد 300ms لحقول البحث، بما في ذلك الحقول التي كانت تستدعي دوال `render...` مباشرة مع كل حرف.
- إضافة Lazy/Virtual Rendering للمنتجات، فواتير المبيعات، فواتير المشتريات، مرتجعات المشتريات، العملاء، الموردين، المصاريف، الموظفين، العمال، المناديب، الحسابات، المخازن، النواقص، دفتر القيود، السندات، الوحدات، العروض، مجموعات العملاء وسجلات النقل.
- إضافة API موحد للاستعلام المحلي بالصفحات `queryRecords` يدعم `offset/limit` والبحث والفرز مع Worker للبيانات الكبيرة.
- إضافة معاملات ذرّية محلية متعددة المجموعات مع سجل استعادة WAL؛ حفظ/حذف فواتير البيع والمشتريات ومرتجعات المشتريات وتحديث المخزون والعملاء/الموردين والصناديق يتم كوحدة واحدة قابلة للاستعادة بعد الانقطاع.
- إبقاء رفع التعديلات إلى Firebase عبر مصالحة ETag للعقدة الكاملة عند وجود طابور رفع، للحفاظ على عدم تداخل التحديثات بين الأجهزة، بينما أصبحت عمليات القراءة اليومية مجزأة حسب Dataset.
- إضافة `.indexOn` لحقول `key`, `companyKey`, `tenantId`, `companyId` داخل `cashTopAdmin/companies` في ملفات قواعد Firebase لتسريع استرجاع المفتاح الاحتياطي.

# Revision 31 - إصلاح إنشاء مفاتيح الشركات من لوحة الأدمن

- إصلاح الخطأ `licenses.filter is not a function` الذي كان يظهر عند حفظ مفتاح شركة عندما تكون بيانات `cashtop_admin_licenses` محفوظة بصيغة Object قديمة بدلاً من Array.
- إضافة تطبيع تلقائي لبيانات المفاتيح والمستخدمين يدعم Array وObject والسجل المفرد وJSON المشفر أكثر من مرة وصيغة Firebase المغلفة.
- تطبيق نفس الحماية على `cashtop_admin_users` و`cashtop_tenant_bindings` لمنع ظهور خطأ مماثل بعد تجاوز مرحلة المفاتيح.
- الحفاظ على المفاتيح القديمة أثناء تحويل الصيغة، ثم إضافة المفتاح الجديد ومدير الشركة إلى القوائم الموحدة بدون حذف السجلات السابقة.
- إضافة تراجع آمن لحالة لوحة الأدمن عند فشل عملية الحفظ، لمنع بقاء المفتاح محجوزاً داخل الذاكرة بعد عملية إنشاء غير مكتملة.
- تقوية قراءة بيانات المفاتيح في صفحة الدخول و`cashtop-core.js` حتى تبقى المفاتيح القديمة والجديدة قابلة للدخول بعد ترقية البيانات.
- تحديث كاش التطبيق إلى `v38-admin-key-create-fix-cache` ورفع نسخة `admin.js` إلى `v20`.

# Revision 30 - دفعات العملاء والدين اليدوي

- إعادة أيقونتي **دفعة** و**دين يدوي** إلى عمود الإجراءات في صفحة العملاء.
- إضافة نافذة تسجيل دفعة تعرض العميل ورصيده، مبلغ الدفعة، الصندوق/الحساب المستلم، التاريخ والملاحظات مع معاينة الرصيد الجديد.
- عند تسجيل الدفعة يُزاد رصيد الصندوق المختار، ويُنقص رصيد العميل، ويُنشأ سند قبض مرتبط بالعميل وحركة في سجل الصندوق.
- إضافة نافذة دين يدوي تزيد ذمة العميل من دون التأثير على الصناديق، وتحفظ المرجع والتاريخ والملاحظات داخل سجل العميل المالي.
- ربط الدفعات بمجموعات `cashtop_vouchers` و`cashtop_funds_db` و`cashtop_customers` المدارة مركزياً، لذلك تدخل تلقائياً في طابور مزامنة Firebase الخاص بالشركة والفرع.
- تحديث سجل العميل لتمييز الحركة باسم **دين يدوي** وعرض مرجعها وملاحظاتها بدلاً من الوصف القديم العام.
- تحديث كاش التطبيق إلى `v37-customer-payment-debt-sync-cache` لضمان وصول صفحة العملاء المعدلة فور نشر الإصدار.

# Revision 29 - إصلاح قراءة الباركود على iPhone

- إصلاح سبب فتح كاميرا الآيفون بدون فك الباركود: تم تعطيل مسار `BarcodeDetector` الأصلي داخل `html5-qrcode` على iPhone/iPad وإجبار المكتبة على استخدام محرك ZXing لقراءة باركودات المتاجر.
- تحديد صيغ الباركود العملية المستخدمة في نقاط البيع: EAN-13 وEAN-8 وUPC-A وUPC-E وCODE-128 وCODE-39 وCODE-93 وCODABAR وITF مع QR وData Matrix.
- توسيع منطقة القراءة على iOS بشكل ديناميكي لتناسب الباركودات الطويلة، وتقليل FPS على WebKit لرفع ثبات فك الترميز.
- محاولة تفعيل التركيز المستمر للكاميرا عندما يسمح WebKit بذلك، بدون فرض Zoom قد يسبب ضبابية على كاميرات iPhone Pro.
- تطبيق الإصلاح على الكاشير والمنتجات والمشتريات، وكذلك قراءة الباركود من الصورة الاحتياطية.
- تحديث كاش التطبيق إلى `v36-ios-barcode-decode-fix-cache` وتحديث نسخة `barcode-tools.js` إلى `v20`.

# Revision 28 — دعم كاميرا الباركود على iPhone وكاش أسرع

- إعادة بناء تشغيل كاميرا الباركود ليكون متوافقاً بصورة أفضل مع iPhone/iPad وAndroid والكمبيوتر، مع استخدام الكاميرا الخلفية تلقائياً متى توفرت.
- إزالة طلب الكاميرا المزدوج قبل تشغيل الماسح؛ أصبح البث يطلب الصلاحية مرة واحدة فقط، وهو مهم لاستقرار WebKit على أجهزة Apple.
- تثبيت `html5-qrcode` على الإصدار `2.3.8` بدلاً من رابط غير محدد الإصدار، وحفظه في كاش الملفات الخارجية ليعمل بعد أول تحميل ناجح حتى عند انقطاع الإنترنت.
- إضافة خصائص `playsinline` و`webkit-playsinline` للفيديو لمنع انتقال الكاميرا إلى تشغيل فيديو بملء الشاشة على iPhone.
- إضافة اختيار تلقائي للكاميرا الخلفية مع بدائل متدرجة عندما لا يقبل الجهاز إعدادات الكاميرا الأساسية.
- إضافة وضع احتياطي: عند رفض/تعذر البث المباشر تظهر إمكانية فتح كاميرا الجهاز والتقاط صورة للباركود ثم قراءتها، بدلاً من إغلاق نافذة المسح بالكامل.
- إضافة ترويسات Firebase Hosting للسماح باستخدام الكاميرا من نفس مصدر التطبيق ومنع طلب الميكروفون غير المطلوب.
- تحديث كاش التطبيق إلى `v35-ios-camera-fast-cache`.
- تعديل Service Worker ليعرض كل صفحة محلية من الكاش أولاً حتى مع توفر الإنترنت، ثم يحدث النسخة المخزنة في الخلفية تحت رابط ثابت بلا معاملات `?v=` حتى لا تبقى نسخة قديمة مخفية في الكاش.
- جعل تثبيت Service Worker الجديد يركز على حزمة الصفحات المحلية أولاً لتفعيل النسخة بسرعة، ثم تسخين مكتبات الخطوط والباركود والتصدير في الخلفية مرة واحدة فقط.
- بدء تسجيل Service Worker من صفحة الدخول فوراً بدلاً من انتظار حدث `load`، مع طلب تدفئة الكاش بعد جاهزيته.

# Revision 27 — نقل فعلي بين الفروع والمخازن وصلاحية الموظف

- إضافة صلاحية `inventory.transfer` للموظفين بصياغة واضحة؛ الموظف أو مدير الفرع المصرح له يستطيع النقل من فرعه فقط، بينما مدير الشركة يستطيع اختيار أي فرع مصدر وأي فرع وجهة.
- السماح بفتح صفحة الفروع لحساب يملك صلاحية النقل حتى لو لم يملك صلاحية إدارة الفروع، مع إبقاء إنشاء/تعديل/حذف الفروع محمية بصلاحياتها الأصلية.
- إعادة بناء نافذة نقل الفروع لتحتوي: الفرع المصدر، المحل الرئيسي/المخزن المصدر، الفرع المستلم، المحل الرئيسي/المخزن المستلم.
- قراءة المنتجات والمخازن من مجموعة الشركة الكاملة أثناء النقل، لذلك يظهر منتج أضيف في فرع 2 عند اختيار فرع 2 حتى لو كانت جلسة المدير على الفرع الرئيسي.
- عرض المنتجات ذات الرصيد الفعلي فقط في موقع المصدر المختار، مع دعم المنتجات متعددة المقاسات والألوان والباركود الخاص بالمقاس.
- دعم النقل بالكرتونة أو بالقطعة حسب إعداد المنتج، بما في ذلك الأصناف متعددة المقاسات عند وجود `piecesPerUnit`.
- خصم الكمية من المحل أو المخزن المصدر وإضافتها إلى المحل أو المخزن الوجهة، وتسجيل الفرع والموقعين والمستخدم في سجل نقل الفروع.
- عند وصول صنف جديد إلى فرع آخر عبر مخزن أو محل، يتم تفعيل `branchCatalog` للفرع المستلم ليظهر الصنف في صفحة المنتجات لذلك الفرع مباشرة.
- إصلاح عزل `storeStocks` بين الفروع داخل إسقاط بيانات المنتجات ودمجها، لمنع فرع من قراءة أو الكتابة فوق أرصدة مخازن فرع آخر عند حفظ المنتجات.
- إضافة كتابة آمنة لمجموعة الشركة الكاملة `setRawCompanyDataset` للعمليات العابرة للفروع مع تحديث الميتاداتا وطابور مزامنة Firebase.
- تحديث كاش التطبيق إلى `v34-cross-branch-warehouse-transfer` ورفع إصدار `cashtop-core.js` إلى `v22`.

# Revision 26 — عزل كامل لكل مفتاح شركة

- إضافة `tenantId` ثابت لكل شركة؛ المفتاح أصبح وسيلة دخول فقط ولا يُستخدم كمسار بيانات قابل للتبديل.
- كل بيانات الشركة المحلية تُحفظ حصراً تحت مساحة `cashtop_data::{tenantId}::*` وبيانات المزامنة تحت نفس المعرّف.
- إنشاء كل شركة جديدة من لوحة المشرف يولد مساراً مستقلاً جديداً في Firebase، مع ربط `keyIndex` بالمستأجر نفسه.
- منع تحميل أي عقدة Firebase إذا كان `tenantId` أو مفتاح الشركة داخل العقدة لا يطابق جلسة الدخول الحالية.
- إلغاء اختيار مسارات تاريخية مبنية على مفتاح الشركة أو معرّف مختلف؛ يسمح فقط بالجذور القديمة التي تحمل نفس `tenantId`.
- إضافة توجيه محلي `cashtop_tenant_bindings` لمنع إعادة استخدام مفتاح قديم من فتح بيانات شركة سابقة على نفس الجهاز.
- حماية ترحيل بيانات `localStorage` القديمة غير المعزولة؛ لا يمكن لشركة جديدة الاستيلاء تلقائياً على بيانات شركة موجودة.
- عزل جلسة كل تبويب بواسطة `sessionStorage` حتى لا يؤدي فتح شركتين في تبويبين مختلفين إلى تبديل مساحة البيانات أثناء العمل.
- تحديث قواعد Realtime Database للتحقق من أن `meta.tenantId` أو `meta.companyId` يطابق اسم عقدة الشركة.
- تحديث تنسيق النسخة الاحتياطية إلى `cashtop-backup-v4` مع تضمين `tenantId` و`companyKey` والتحقق منهما عند الاستعادة.
- تحديث كاش التطبيق إلى `v33-tenant-isolation` ورفع إصدارات ملفات Core/Login/Admin/Sync لإجبار المتصفح على تحميل النسخة الجديدة.

# Revision 23 - إصلاح المخازن والفروع والصندوق الافتراضي

- إصلاح عدم ظهور/حفظ المخازن بعد ربط البيانات بالفروع؛ أصبحت المخازن ذات `branchId` صالحة وتظهر في المخازن والمشتريات والمنتجات.
- تقوية النقل بين المحل والمخازن مع التحقق من الرصيد الفعلي لحظة التنفيذ ومنع القيم غير الصالحة، مع تسجيل المصدر والوجهة.
- إنشاء/تثبيت **الفرع الرئيسي** تلقائياً كفرع أول ورئيسي ونشط، ومنع حذفه أو تجميده أو تغيير اسمه.
- ربط جلسة مدير الشركة بـ **الفرع الرئيسي** لمنع ظهور «فرع غير معروف».
- إنشاء **صندوق الكاش** تلقائياً لكل فرع عند الحاجة، ومنع حذفه وتثبيت اسمه ونوعه كصندوق كاش.
- تحديث إصدار Cache الخاص بتطبيق PWA لضمان تحميل الملفات المعدلة.

# Revision 20

- توحيد تمركز جميع النوافذ المنبثقة بين الموقع والتطبيق المثبت.
- إصلاح تموضع النوافذ الثابتة داخل الصفحة المضمنة؛ كان التحويل الرسومي للصفحة يجعل نافذة الكاميرا وبعض النوافذ الطويلة تلتصق بأعلى الشاشة داخل PWA.
- جعل النوافذ على الجوال تتمركز داخل المساحة المرئية بين الهيدر وشريط التنقل السفلي.
- الحفاظ على نافذتي الكاميرا والماسح بحجم مدمج ومتناسق على الهاتف.
- تحديث إصدار كاش التطبيق لضمان وصول إصلاحات الواجهة إلى النسخة المثبتة.

# Revision 18 — كاش دائم ومزامنة مستقلة لكل شركة

- إعادة بناء Service Worker ليحفظ جميع صفحات النظام وملفات JavaScript وCSS والصور والصوت وملف التطبيق كحزمة محلية مكتملة؛ لا يُفعّل الإصدار الجديد إذا كانت الحزمة المحلية ناقصة.
- فتح صفحات وأصول التطبيق من الكاش أولاً دائماً، دون فحص الشبكة أو تحديث صامت عند الانتقال بين الأقسام.
- طلب التخزين الدائم من المتصفح لحماية Cache Storage وبيانات الشركة المحلية من الإزالة التلقائية بعد إعادة تشغيل الجهاز.
- اكتشاف سبب خلل المزامنة: كان Service Worker يعترض طلبات GET إلى Firebase ويعيد نتيجة قديمة من الكاش، لذلك لا يرى الجهاز تعديلات الأجهزة الأخرى. أصبحت جميع طلبات Realtime Database وAuthentication تمر مباشرة إلى الشبكة ولا تُخزن مطلقاً.
- تثبيت مسار Firebase على `cashTopExchange/cashTopPOS/{companyId}` لكل شركة، ومنع اختيار مسار شركة أو مفتاح آخر بناءً على أحدث تاريخ. المسارات القديمة لا تستخدم إلا إذا أثبتت بياناتها أنها تخص المفتاح الحالي.
- عزل حالة المسار وطابور المزامنة لكل `companyId`، واختبار مفتاحين مختلفين ورفع وسحب بيانات كل شركة دون اختلاط.
- جعل لوحة المشرف تكتب مجموعة `cashtop_company_access` وحدها بدلاً من استبدال عقدة الشركة كاملة، حتى لا يؤدي فتح أو قفل استيراد النسخ إلى مسح أو إيقاف بيانات متزامنة.
- دمج حقول الخطة والحالة وقفل الاستيراد المرجعية من لوحة المشرف عند وجود تعديل محلي معلّق، مع استمرار مزامنة بقية البيانات بشكل طبيعي.
- عدم رفع مجموعات فارغة مزروعة تلقائياً عند إنشاء مفتاح جديد؛ يبدأ الرفع عند وجود تعديل حقيقي، مما يسرّع أول مزامنة للشركة الجديدة.
- تحديث مخطط المزامنة إلى `18` وكاش التطبيق إلى `v28-revision-18`.

# Revision 17

- إصلاح خصم مخزون الفرع الرئيسي: أصبحت القيمة `MAIN` تُعامل كفرع رئيسي فعلي، ويُخصم العدد الصحيح من `stockPieces` سواء كانت الكمية قطعة أو كرتونة.
- التحقق من ثبات الخصم في التخزين المحلي قبل إكمال قيد الفاتورة، مع التراجع الكامل عند فشل أي جزء.
- استمرار الحفظ والعمل دون إنترنت؛ تعديل المنتجات يُضاف إلى طابور المزامنة ويتزامن عند عودة الاتصال.
- زر **حفظ** لا يطبع مطلقاً؛ الطباعة تتم فقط من زر **حفظ وطباعة**.
- فتح الصفحات والأصول المحلية من الكاش أولاً على الجوال والكمبيوتر، مع تحديث الكاش عند إصدار نسخة جديدة.
- إخفاء مؤشرات التحميل البرمجية الشائعة وتحسين سلاسة التحريك والرسم.
- لم يُطبّق الشكل المطابق للصورة رقم 1 لأن صورة مرجع الفاتورة لم تكن مرفقة مع الطلب.

# Revision 16 — مسودة ترحيل المبيعات والاقتراحات والصناديق

- حفظ بيانات نافذة «ترحيل فواتير مبيعات» تلقائياً في مسودة محلية مرتبطة بالشركة والفرع، واستعادتها بعد إغلاق النافذة أو إعادة فتح الصفحة؛ لا تُحذف المسودة إلا بعد نجاح الترحيل.
- استبدال قوائم `datalist` في اسم العميل والصنف باقتراحات مرئية مباشرة تظهر بمجرد كتابة حرف، من دون سهم قائمة.
- تعبئة رقم جوال العميل تلقائياً عند اختياره من الاقتراحات، مع استمرار إنشاء العميل الجديد تلقائياً عند كتابة اسم غير مسجل.
- عرض اقتراحات المنتجات بالاسم والباركود والرصيد، وتعبئة سعر المنتج عند اختياره.
- حفظ المنتج غير الموجود أو غير المتوفر بالمخزون داخل الفاتورة كصنف حر، دون منع الترحيل أو تنفيذ خصم مخزون جزئي.
- إضافة اختيار صندوق مستقل لكل فاتورة؛ يُضاف المبلغ المدفوع إلى الصندوق المختار مع حركة مالية مرتبطة برقم الفاتورة.
- ربط الدين بالعميل وبالفاتورة نفسها عبر قيمة `debt` في الفاتورة وسجل `debtInvoices` داخل العميل، مع إزالة الربط عند حذف الفاتورة وعكس حركتها.
- إنشاء عميل مؤقت مرتبط بالفاتورة عند وجود دين وعدم إدخال اسم، حتى لا يبقى أي دين بلا عميل مع بقاء الحقول اختيارية.
- إبقاء تخطيط الحقول زوجياً على الجوال بإضافة حقل حالة الدفع، والتحقق بصرياً عند عرض 390 بكسل دون تمرير أفقي.
- تحديث مراجع صفحة الفواتير إلى `v16` وService Worker إلى Cache `v24`.

# Revision 15 — ترحيل جماعي لفواتير المبيعات

- إضافة زر «ترحيل فواتير مبيعات» داخل صفحة فواتير المبيعات، مع نافذة متجاوبة تسمح بإدخال عدة فواتير في عملية واحدة.
- حفظ كل بطاقة مدخلة كفاتورة مستقلة برقم فريد داخل سجل الفواتير، بدلاً من دمجها في فاتورة واحدة.
- جعل جميع حقول الترحيل اختيارية: اسم العميل، رقم الجوال، التاريخ، الأصناف، الكمية، السعر والمدفوع.
- دعم إضافة عدة أصناف داخل كل فاتورة، مع حساب إجمالي السطر وإجمالي الفاتورة والباقي كدين تلقائياً.
- عرض اقتراحات العملاء المسجلين أثناء كتابة الاسم، وتعبئة رقم الجوال عند التطابق، وإنشاء العميل تلقائياً إذا كان الاسم جديداً.
- تسجيل المتبقي على رصيد العميل، وترحيل المبلغ المدفوع إلى الصندوق الافتراضي مع حركة حساب مستقلة لكل فاتورة.
- ربط الصنف بالمخزون عند تطابق اسمه مع منتج عادي مسجل ووجود كمية كافية؛ الاسم الحر أو الصنف ذو المقاسات يحفظ كسطر مخصص دون حركة مخزون غير آمنة.
- تصميم الحقول على عمودين في الجوال، وكذلك حقول كل صنف، مع نافذة كاملة الارتفاع مناسبة للشاشات الصغيرة.
- حماية عملية الترحيل بلقطة تراجع؛ عند فشل أي كتابة تعاد الفواتير والعملاء والمخزون والحسابات إلى حالتها السابقة.
- تحديث نسخة كاش صفحة الفواتير إلى `v15` وService Worker إلى Cache `v23`.

# Revision 14 — التصنيع المتقدم والنسخ المقفلة وتحسين الجوال

- نقل عرض خطة الشركة إلى صفحة إعدادات النظام فقط، وإزالة تفاصيل العدّادات والمدة المتبقية والتاريخ من واجهة الاشتراك والتنبيه.
- جعل Firebase المصدر المرجعي للخطة وحالة المفتاح عند تسجيل الدخول عبر الإنترنت، مع تحديث الخطة المحلية من سجل الشركة لمنع اختلاف Plus وPro بين الأجهزة.
- حذف أي رابط أو إشارة مرئية إلى إدارة الشركات والمفاتيح من صفحة دخول الشركات؛ تبقى لوحة المشرف متاحة بفتح `admin.html` مباشرة.
- جعل جدول لوحة المشرف بطاقات متجاوبة على الجوال، وإضافة زر نسخ للمفتاح، وخيار فتح أو قفل استيراد النسخ لكل مفتاح ومزامنته في Firebase.
- إضافة روابط نسخ احتياطي مستقلة داخل كل مجموعة في القائمة الجانبية، مع تصدير واستيراد احترافي لكل قسم أو للشركة كاملة.
- جعل الاستيراد مقفلاً افتراضياً؛ وعند فتحه تُكتب البيانات في التخزين المحلي أولاً، ثم تدخل طابور العمليات المعلقة وتبدأ مزامنة Firebase مباشرة.
- ترقية تنسيق النسخ إلى `backup-v3` و`section-v3` لحفظ قيمة `localStorage` الأصلية حرفياً، بما في ذلك قوالب الرسائل النصية، مع استمرار استيراد نسخ v2 القديمة.
- إعادة بناء التصنيع ليقبل كتابة اسم المنتج النهائي والمكونات مع اقتراحات فورية بدلاً من القوائم المنبثقة.
- إنشاء المنتج المصنع كمنتج مخزون فعلي، مع باركود وزر توليد وسعر بيع، وظهوره تلقائياً في الكاشير.
- احتساب استهلاك كل مادة لكل قطعة منتجة، وخصمها من المخزون، واحتساب تكلفة القطعة والدورة والربح ومتوسط تكلفة المنتج النهائي.
- توحيد لون نص توست نجاح التصنيع باللون الأبيض فوق الخلفية الخضراء.
- إضافة الراتب الشهري ويوم الاستحقاق وحساب الصرف للموظف، وإنشاء إشعار استحقاق مع زر «قبض» يخصم من الحساب ويسجل الدفعة ويزامنها.
- إصلاح أثر مرتجع المشتريات على رصيد المورد، وتسجيل حركة المرتجع والمسترد النقدي وعكسهما عند حذف المستند.
- عرض أسماء أصناف فاتورة المشتريات مفصولة بفاصلة عربية في سجل المورد بدلاً من المرجع التقني.
- دعم دقة `0.001` في تكلفة وسعر المنتجات والمشتريات والمرتجعات والكاشير، بما في ذلك حساب سعر الحبة داخل الوحدة.
- جعل جداول المقاسات والأصناف الفرعية قابلة للسحب أفقياً بدلاً من ضغط الحقول على الجوال.
- إظهار الفرع الرئيسي الأصلي صراحةً ضمن خيارات النقل بين الفروع.
- جعل مجموعات القائمة الجانبية تعمل كأكورديون؛ فتح مجموعة يغلق المجموعة السابقة، مع أيقونات رمادية غير مظللة افتراضياً.
- إضافة WhatsApp وSMS إلى إجراءات جميع فواتير المبيعات باستخدام قالب الرسالة ومتغيراته من الإعدادات.
- جعل «تعديل الكمية حسب السعر» متوقفاً افتراضياً، وعند تفعيله يصبح مبلغ السطر هو المدخل وتُحسب الكمية المقابلة تلقائياً.
- تحديث Service Worker إلى Cache `v22` بسياسة Cache First للصفحات والأصول، مع تخزين الخطوط وأيقونات Font Awesome ومكتبات الواجهة للعمل دون اتصال.
- تحديث مخطط مزامنة Firebase إلى `14` ومراجع الأصول إلى `v14`.

# Revision 13 — تعدد الشركات وعزل الفروع والتصنيع

- إضافة لوحة `admin.html` لإنشاء شركات متعددة بمفتاح فريد ومدير وخطة ومدة انتهاء وحالة قابلة للإيقاف والتعديل.
- إزالة إنشاء مفتاح `CASHTOP-DEMO` وحذف بياناته التجريبية القديمة عند فتح صفحة الدخول.
- إضافة خطتي Plus وPro وتطبيق الحدود الثابتة واليومية، مع أشرطة استهلاك ومدة اشتراك.
- فصل التخزين المحلي ومسار Firebase حسب `companyId`، ومنع رفع بيانات فرع واحد فوق بيانات بقية الفروع.
- عزل رصيد المنتجات والمقاسات والدفعات المنتهية حسب الفرع، مع دمج تعديل صفحة المنتجات والكاشير في سجل الشركة الكامل دون تغيير مخزون فرع آخر.
- توحيد الفرع الرئيسي على مساحة البيانات `MAIN` مع الاحتفاظ برقم سجل الفرع لمديره وموظفيه.
- تطبيق حدود Plus اليومية كما طُلبت: فواتير البيع لكل فرع، والمصروفات والعملاء الجدد وفواتير المشتريات على مستوى الشركة.
- قصر آخر خمس فواتير في لوحة التحكم على الفرع الحالي.
- بدء الموظف الجديد دون صلاحيات محددة.
- تحديث صلاحيات الموظف ومدير الفرع وحالتهما تلقائياً، وإخراجهما فور الحذف أو الإيقاف بعد وصول تحديث Firebase.
- السماح لكل مستخدم بفتح الإعدادات مع إظهار تسجيل الخروج فقط لمن لا يملك صلاحية الإعدادات.
- إعادة تنظيم القائمة الجانبية ضمن المبيعات والمشتريات والمخزون والتصنيع والمالية والموارد والتقارير والإعدادات.
- استبدال صفحة إدارة التصنيع وربط وصفات التصنيع وأوامره بالمخزون والتكلفة والربح والقيود والمزامنة.
- إضافة فهرس Firebase للمفاتيح تحت `cashTopExchange/cashTopAdmin` واستخدام مشروع `meopp-8f1fa` نفسه للتراخيص والبيانات.
- إضافة مصادقة Anonymous احتياطية لقراءة بيانات الدخول عند استخدام القواعد الآمنة.
- تحديث Service Worker إلى Cache `v21`.

# Revision 11 — ثبات المزامنة والفواتير والفروع

- إصلاح خطأ Firebase `Invalid data / couldn't parse key` الناتج عن مفاتيح الصلاحيات الدقيقة مثل `sales.create`؛ أصبحت كل مجموعة بيانات تُرفع كقيمة JSON نصية واحدة داخل غلاف آمن، ثم تُعاد كما هي إلى التخزين المحلي.
- حماية المزامنة من سباق الشبكة: إذا أضيف موظف أو فرع أو فاتورة أثناء رفع جارٍ، لا تُمسح العملية الأحدث من الطابور ولا يكتب السحب القديم فوق التعديل المحلي.
- إعادة تحميل الفروع والموظفين من المصفوفات أو الخرائط القديمة، وتحديث جدول الفروع وقائمة اختيار مدير الفرع مباشرة بعد الحفظ وبعد سحب Firebase.
- عرض آخر خمس فواتير بيع في لوحة التحكم، مرتبة من الأحدث إلى الأقدم، مع تحديث تلقائي بعد الحفظ أو الحذف أو المزامنة.
- تعديل الفاتورة الحرارية بحيث يظهر التاريخ مقابل الكاشير في سطر، والعميل مقابل طريقة الدفع في سطر آخر، مع استمرار مقاسي 58 و80 مم.
- إضافة قالب صورة رسمي موحد لفاتورة المبيعات والمشتريات، بترويسة وشعار وجدول أصناف وملخص وملحوظات وتوقيع مطابق لبنية النموذج المرفق.
- إضافة زري WhatsApp وSMS للفواتير المعلقة أو غير المكتملة، وقالب رسالة مستقل في إعدادات العملاء يدعم متغير الأصناف `{items}` وبقية متغيرات الفاتورة.
- جعل حفظ فاتورة الكاشير وحذف الفاتورة وعكسها عمليتين محميتين من التكرار والتوقف الجزئي، مع استعادة المخزون والحسابات تلقائياً عند حدوث خطأ.
- توحيد مقارنة معرفات المنتجات النصية والرقمية في الكاشير لمنع فشل الإضافة أو عدم تحرك المخزون بعد المزامنة أو الاستيراد.
- إضافة زر رابط بجانب شعار الشركة لفتح نافذة إدخال رابط صورة وحفظه مباشرة ضمن إعدادات النظام والمزامنة.
- تحديث Service Worker إلى Cache `v19`.

# المراجعة الخامسة

- نقل رقم الإشعارات إلى يسار أيقونة الجرس.
- استبدال أيقونة التطبيق بالصورة المرفقة وتحديث مقاسي PWA.
- إعادة أيقونة المزامنة السحابية من النسخة السابقة.
- جعل خلفية عداد عمليات المزامنة خضراء، والعداد يعكس طابور التعديلات المحلية فقط.
- منع الكتابات المتطابقة عند دخول الصفحات من إنشاء عمليات مزامنة وهمية.
- دمج تعديلات مجموعة البيانات نفسها في عملية معلقة واحدة مع حماية التعديلات التي تحدث أثناء الرفع.
- إخفاء توست نجاح المزامنة التلقائية؛ يظهر النجاح فقط عند الضغط على زر المزامنة.
- تنفيذ سحب أولي كامل من Firebase مرة واحدة، ثم فحص meta خفيف وسحب عند وجود تحديث فعلي.
- استبدال «أونلاين» بـ «متصل» و«أوف لاين» بـ «غير متصل».
- منع فتح القائمة المنسدلة أثناء السحب/التمرير؛ لا تفتح إلا بنقرة مقصودة.

# سجل تعديلات كاش توب

## الكاشير والأسعار

- إصلاح تطبيق أسعار مجموعات العملاء من سجل المجموعة نفسه.
- اختيار العميل تلقائياً عند كتابة الاسم أو رقم الهاتف بشكل مطابق.
- تحديث سعر بطاقات المنتجات وأسعار عناصر السلة فور اختيار العميل.
- دعم سعر خاص للقطعة والوحدة.
- إضافة عروض المبيعات والكوبونات وتسجيل مرات الاستخدام عند إصدار الفاتورة فقط.
- إخفاء حقل الكوبون عندما لا توجد كوبونات نشطة.
- إضافة ضريبة المبيعات إلى الملخص والفاتورة حسب إعدادات التحمل.

## المنتجات والمخزون

- إضافة دوال تعديل وحذف المنتجات المفقودة.
- حذف المنتج مباشرة دون إنشاء قيد عكسي أو حركة مالية.
- إصلاح حفظ باركود القطعة داخل الوحدة.
- تحميل الفروع والمخازن الفعلية في النقل المخزني ومنع النقل إلى نفس الموقع.

## الواجهة

- تطبيق هيكل الهيدر والقائمة الجانبية والشريط السفلي على صفحات النظام.
- إخفاء الشعار من الهيدر والإبقاء عليه في القائمة الجانبية.
- تحويل أيقونات الهيكل إلى SVG داخلي.
- تحديث القوائم المنسدلة بنمط موحد وحديث.
- إعادة تنظيم العملاء والموردين والمستخدمين والإدارة والإعدادات.

## التقارير

- حذف اسم الموظف التجريبي الثابت.
- إنشاء أداء الموظفين من بيانات الفواتير الفعلية.
- إصلاح تقرير الموردين ليحسب المشتريات والمدفوعات والديون اليدوية.
- جعل الرسم البياني يتجمع حسب الفترة المختارة مع نقاط وخط صاعد وهابط.
- دمج السجلات المؤرشفة في التقارير حتى لا تتغير النتائج بعد التنظيف.

## التصنيع

- استبدال صفحة التصنيع القديمة بصفحة وصفات وأوامر تصنيع مستقلة.
- خصم المواد الخام وإضافة المنتج النهائي وحساب متوسط تكلفته.
- عدم إنشاء فاتورة مشتريات عند تنفيذ التصنيع.

## صفحات جديدة

- `sales-offers.html` — عروض المبيعات وإنشاء كوبون.
- `coupons.html` — إدارة الكوبونات.
- `tax-settings.html` — ضريبة المبيعات والمشتريات ومن يتحملها.
- `notifications.html` — نفاد المخزون، تأخر الدين، وعدم نشاط العميل.
- `storage-settings.html` — حدود السجلات والأرشفة إلى IndexedDB.

## Firebase والتخزين

- ربط مشروع Firebase السابق `meopp-8f1fa`.
- المزامنة عبر Realtime Database تحت `cashTopExchange/cashTopPOS/{companyId}`.
- إضافة `database.rules.json` وربطه بملف `firebase.json`.
- تشغيل Local-First مع محاولة Anonymous Authentication للمزامنة.
- نقل السجلات المكتملة القديمة إلى IndexedDB وتخفيف البيانات النشطة في Firebase.


## مراجعة الواجهات والقوائم
- إعادة مظهر حقول القوائم المنسدلة والكاشير إلى تصميم الصفحات الأصلي، مع قائمة خيارات حديثة مخصصة عند الفتح.
- استبدال نقطة الاتصال بكلمة «أونلاين / أوف لاين» على جميع أحجام الشاشات.
- إعادة الشريط السفلي للجوال إلى التصميم والروابط المرسلة.
- حذف صفحة الكوبونات وروابطها من النظام.
- إعادة بناء صفحة عروض المبيعات وفق التصميم المرسل، مع الحفظ والتعديل والحذف والتصدير والتطبيق في الكاشير.
- ربط النقل من صفحة المنتجات بالمستودعات الفعلية وإضافة الكمية إلى رصيد المستودع الوجهة.


## المراجعة الثالثة — كاش توب 2
- تثبيت شريط الكاشير السفلي على حافة نافذة اللابتوب مع التوقف عند القائمة الجانبية.
- تصغير النوافذ المنبثقة وربط القوائم الحديثة بالحقول على الجوال والكمبيوتر.
- شريط جوال جديد: الرئيسية، الكاشير، المنتجات، العملاء، الفواتير.
- طابور مزامنة محلي بعدّاد متناقص وتوست «تمت المزامنة».
- دعم العمل دون إنترنت وسياسة Cache First لجميع صفحات التطبيق.
- دعم كسور المخزون والكمية وتعديل الكمية تلقائياً حسب المبلغ المدخل.
- تطبيق قالب الرسائل على WhatsApp وSMS مع متغيرات العميل ومقدمة WhatsApp 972.
- تحديث الاسم إلى كاش توب 2 وأيقونة التطبيق إلى الصورة المرفقة.

## المراجعة الرابعة — قائمة الكاشير والمزامنة
- إعادة القوائم المنسدلة إلى سلوك وتصميم المراجعة السابقة.
- استبدال أيقونة المزامنة بسهمين دائريين احترافيين مع دوران بطيء ثم متسارع عند الضغط.
- تبديل موضعي الإشعارات والمزامنة في الهيدر.
- إغلاق القائمة الجانبية افتراضياً في كاشير اللابتوب ومد صفحة الكاشير إلى كامل العرض.
- إظهار زر القائمة ذي الثلاث شحطات في هيدر الكاشير على اللابتوب.
- فتح القائمة الجانبية كطبقة فوق شريط الكاشير السفلي دون تحريك المحتوى.
- إعادة شريط الكاشير السفلي إلى تنسيقه السابق وتثبيته عند الحافة السفلية للموقع.

## Revision 8 — إصلاح Firebase وثبات الواجهات

- تغيير الاتصال إلى Database-First لمنع `CONFIGURATION_NOT_FOUND` من إيقاف المزامنة.
- فحص المسار التاريخي `cashTopExchange/cashTopPOS` ومسار Revision 7، مع تجربة `companyId` و`companyKey`.
- عدم محاولة Anonymous Authentication إلا بعد رفض Realtime Database الوصول.
- إضافة قواعد آمنة مدمجة وقواعد توافق منفصلة للمشروع الذي لا يحتوي إعداد Authentication.
- حماية الكاشير من قيم JSON القديمة أو التالفة، وربط تبويبات المنتجات والسلة والبيانات قبل بقية عمليات التحميل.
- إصلاح الطبقات الشفافة والقوائم المنسدلة التي قد تمنع الضغط في WebView.
- إصلاح الحقول المطلوبة المخفية في نافذة المنتجات، وربط زر الإضافة بشكل مستقل عن تحميل الجدول.
- إصلاح فتح نافذة العملاء وربط إجراءات الصفوف دون أوامر `onclick` ديناميكية.
- تحديث Service Worker والكاش لإجبار الأجهزة على استلام ملفات الإصلاح الجديدة.

## Revision 9 — الطباعة والفروع والمخازن والدخول الموحد

- حذف حقل اسم الطابعة من إعدادات الطابعة، واعتماد طابعة النظام الافتراضية تلقائياً من نافذة الطباعة في المتصفح/نظام التشغيل.
- حصر نوع الطباعة في المقاسين الحراريين 58 مم و80 مم، مع عرض الفاتورة بعرض الرول الكامل وهوامش صفرية.
- توسيط شعار الشركة في الفاتورة الحرارية وفي صورة الفاتورة، وانتظار تحميل الشعار قبل الطباعة أو إنشاء الصورة.
- فصل المخازن عن الفروع نهائياً: النقل من صفحة المنتجات يعرض المخازن المستقلة فقط، وصفحة المخازن لا تعرض مخازن الفروع القديمة.
- تخصيص النقل بين الفروع لقائمة الفروع فقط، مع سجل مستقل وأرصدة فروع داخل بيانات المنتجات.
- ربط أول فرع يضيفه المستخدم برصيد المتجر الرئيسي المستخدم في الكاشير، ثم حفظ أرصدة الفروع الأخرى بشكل مستقل.
- حذف سجلات الفروع التجريبية القديمة مرة واحدة، وعدم إنشاء أي فرع أو مخزن افتراضي تلقائياً.
- إضافة مدير مستقل لكل فرع باسم مستخدم وكلمة مرور، والدخول من صفحة تسجيل الدخول نفسها باستخدام مفتاح الشركة نفسه.
- الإبقاء على دخول مدير الشركة والموظفين في الصفحة نفسها، وإزالة صفحة وملفات دخول المشرف العام من حزمة الواجهة.
- مزامنة مديري الفروع والموظفين والفروع وأرصدة النقل وإعدادات الطابعة وكلمات المرور ضمن مجموعات Firebase الحالية.
- تغيير كلمة مرور مدير الشركة أو مدير الفرع أو الموظف يحدّث مجموعة الحساب المناسبة ويشغّل محاولة مزامنة مباشرة.
- تحديث Cache Storage إلى v17، وجعل ملفات الدخول والفواتير وFirebase والنواة Network First لمنع تشغيل نسخة قديمة.
- أصبح الكاشير يقرأ ويخصم رصيد الفرع المرتبط بجلسة مدير الفرع أو الموظف، وتُحفظ الفاتورة مع رقم واسم الفرع.
- عكس الفاتورة عند التعديل أو الحذف يعيد الكمية إلى الفرع الذي صدرت منه الفاتورة، وليس إلى الرصيد الرئيسي دائماً.
- مدير الفرع يرى فواتير فرعه فقط في صفحة الفواتير.
- إضافة اختيار الفرع داخل حساب الموظف؛ الموظف المرتبط بفرع يدخل بنفس مفتاح الشركة ويستخدم رصيد ذلك الفرع في الكاشير.

## Revision 10 — مجموعات العملاء والكمية حسب السعر والباركود

- إصلاح صفحة مجموعات العملاء لعرض جميع العملاء المسجلين وإعادة تحميلهم بعد السحب من Firebase.
- إصلاح نافذة إضافة العميل لتعرض مجموعات العملاء الحالية وتزامن الربط في سجل العميل وسجل المجموعة.
- إضافة سعر خاص للقطعة والوحدة لكل منتج داخل المجموعة، وتطبيقه تلقائياً في الكاشير عند مطابقة اسم العميل أو هاتفه.
- إعادة تسعير عناصر السلة فور تغيير العميل أو وصول تحديث أسعار المجموعات من المزامنة.
- تثبيت زيادة ونقصان السلة على خطوة مقدارها 1.
- إعادة برمجة وضع «الكمية حسب السعر»: المبلغ المدخل يمثل إجمالي السطر، والكمية = المبلغ ÷ سعر القطعة أو الوحدة المختارة، مع دعم الكسور مثل 0.5.
- إضافة مخزن وجهة مستقل لكل سطر في فاتورة المشتريات، وحفظ المخزن مع السطر وعكس المخزون منه عند حذف الفاتورة.
- إضافة أداة باركود مشتركة للكاشير والمنتجات والمشتريات مع دعم `BarcodeDetector` و`Html5Qrcode` عند توفره.
- دعم قارئات الباركود الخارجية التي تعمل كلوحة مفاتيح، والإضافة التلقائية للسلة في الكاشير أو تعبئة حقل الباركود المركّز في النوافذ الأخرى.
- إضافة طلب صلاحية الكاميرا ورسائل خطأ عربية، وإصلاح إغلاق نافذة كاميرا الباركود أثناء التشغيل أو أثناء طلب الإذن.
- إضافة `qr.mp3` وتشغيله عند قراءة الكاميرا فقط، دون تشغيل صوت إضافي لقراءة جهاز الباركود.
- تحديث Service Worker إلى `v18` وتخزين أداة الباركود وملف الصوت للعمل دون اتصال.

## Revision 12 — مزامنة حسابات الفروع، القيود المالية، الصلاحية والهالك

- إصلاح خطأ تسجيل الدخول `context.branches.find is not a function` عبر تطبيع بيانات الفروع والموظفين القادمة من Firebase سواء كانت مصفوفة، خريطة Firebase، نص JSON أو نصاً مشفراً مرتين من نسخة قديمة.
- إصلاح ترحيل بيانات الجهاز الثاني بحيث تُكتب قيمة `local-storage-json-v1` إلى التخزين المحلي دون إعادة تشفيرها، مع إبقاء التوافق مع البيانات القديمة.
- التحقق من مزامنة `cashtop_branches` و`cashtop_employees` وحساب مدير الفرع وكلمات المرور والصلاحيات الدقيقة عبر مسار الشركة نفسه.
- ترتيب نافذة الموردين، حذف كلمتي «إجباري» و«ديون سابقة»، جعل رقم الجوال اختيارياً، وتصغير عناوين الحقول.
- إيقاف اقتراح أسماء المنتجات في حقل اسم المنتج داخل نافذة الإضافة.
- نقل بطاقة تفاصيل المورد والدفع والخصم إلى آخر نموذج المنتج قبل أزرار الحفظ والإلغاء وإضافة آخر.
- تسجيل الرصيد غير المدفوع من إضافة المنتجات كدين للمورد، وربط الحركة بفاتورة الشراء ومصدرها.
- إضافة تاريخ انتهاء ورقم دفعة إلى إضافة المنتج وفاتورة المشتريات، وحفظ دفعات المخزون مع المخزن والتكلفة والكمية المتبقية.
- عكس فاتورة المشتريات من المخزون والمورد والصندوق عند الحذف، مع منع العكس إذا استُهلك جزء من الدفعة، واستعادة لقطة البيانات كاملة عند أي خطأ.
- حذف عبارة «الوحدة الدولية» من نافذة الوحدات، تصغير العناوين، وجعل حقل عدد القطع داخل الوحدة بعرض السطر.
- إصلاح إجماليات الكاشير في شاشة المراجعة والدفع لتُحسب من نموذج السلة مباشرة بدلاً من قراءة النص المعروض.
- إعادة المخزون المؤقت في بطاقات الأصناف: إضافة الصنف تقلل المتاح بصرياً، وزيادة/نقصان السلة تحدث البطاقة، وإلغاء السلة يعيد العدد دون ترحيل فعلي.
- إعادة برمجة سندات القبض والصرف لربطها بالصندوق والعملاء والموردين، مع عكس القيد السابق عند التعديل أو الحذف ومنع الحركات الجزئية.
- إضافة تنبيهات انتهاء الصلاحية وقرب الانتهاء مع عدد أيام قابل للضبط وزر «إضافة للهالك» يخصم المخزون ويسجل تكلفة الهالك كمصروف غير نقدي يخفض صافي الربح.
- تطبيق FEFO على الدفعات الصالحة فقط، واستبعاد الكمية المنتهية المتتبعة من المتاح للبيع. لم يُسمح ببيع منتج منتهي بصمت حفاظاً على سلامة المخزون والعملاء.
- إضافة `cashtop_wastage` إلى مجموعات البيانات المتزامنة مع Firebase وتحديث مخطط المزامنة إلى 12.
- تحديث Service Worker إلى `v20` ووسوم منع الكاش إلى `v12`.
- لم يتم استبدال صفحة إدارة التصنيع في هذه المراجعة لأن الملف/الكود المطلوب استبداله لم يكن ضمن المرفقات؛ بقيت الصفحة الحالية دون تغيير وظيفي.

## Revision 19
- تثبيت واجهة التطبيق على الوضع العمودي عبر Web App Manifest ومحاولة Screen Orientation API داخل وضع التطبيق المثبت.
- إصلاح ارتفاع نافذة التطبيق المثبتة باستخدام Visual Viewport منذ أول فتح، وليس بعد تدوير الجهاز.
- ضمان بقاء آخر عناصر الصفحة وأزرار الإجراءات بالكامل فوق شريط التنقل السفلي مع احتساب Safe Area.
- تحديث قياسات الواجهة عند العودة للتطبيق وتغير أبعاد الشاشة.
- تحديث سياسة فحص Service Worker إلى updateViaCache=none وتسريع وصول ملفات الواجهة الجديدة.

## Revision 22
- تعديل أولوية قراءة الباركود في الكاشير: عند استخدام نفس الباركود للقطعة والكرتونة/الوحدة، تُعتمد القطعة افتراضياً، ولا تُقرأ الكرتونة إلا إذا لم يوجد تطابق كقطعة.

## Revision 24 - تصدير PDF/Excel جدولي احترافي
- إلغاء إنشاء ملفات PDF بطريقة تصوير الشاشة من جميع صفحات التقارير والتصدير التي كانت تستخدم html2canvas ثم addImage.
- إضافة محرك موحد `cashtop-export.js` لإنشاء جداول PDF فعلية متعددة الصفحات باستخدام jsPDF AutoTable مع دعم اتجاه RTL ومحاولة تحميل خط عربي مناسب وقت التشغيل.
- إصلاح PDF الموردين الأبيض وتحويل كشف الموردين وكشف المورد الفردي إلى جداول بيانات فعلية.
- تطوير PDF/Excel العملاء ليشمل البيانات الكاملة: الاسم، الجوال، العنوان، المجموعة، حد الائتمان، حالة الرصيد، الدين، الرصيد الدائن، الرصيد الصافي والملاحظات.
- تحويل سجل العميل PDF إلى جدول تفصيلي للحركات والفواتير والديون والسداد والرصيد التراكمي.
- تحويل PDF للحسابات، المخازن، نقل المخزون، المنتجات، السندات، التقارير، العمال، المشتريات، المصاريف، المناديب، الموظفين ومرتجعات المشتريات إلى تصدير جدولي فعلي.
- تحسين ملفات Excel على مستوى الموقع تلقائياً: اتجاه RTL، ضبط عرض الأعمدة، التفاف النص، واكتشاف صف العناوين وإضافة AutoFilter عند الإمكان.
- الإبقاء على إصلاحات Revision 23 الخاصة بالمخازن والنقل والفرع الرئيسي وصندوق الكاش الافتراضي.


## Revision 31 — تحويل طبقة البيانات إلى MongoDB Atlas API

- استبدال نقطة اتصال Firebase RTDB المباشرة بـ `https://cash-top-api-2026.vercel.app/api/rtdb`.
- إضافة محول تلقائي من مسارات RTDB بصيغة `.json` إلى `?path=` الخاصة بالـ API الوسيط.
- إبقاء `MONGODB_URI` و`MONGODB_DB` على الخادم فقط وعدم تضمين أسرار MongoDB في الواجهة.
- تحويل تسجيل الدخول وفحص المفتاح وfallback `keyIndex -> companies` للعمل عبر MongoDB API.
- تحويل لوحة إدارة الشركات والمفاتيح إلى MongoDB API، مع توافق لعملية PATCH عبر GET+PUT.
- إبقاء عزل الشركات الحالي حسب `tenantId` ومسارات Revision 31 دون تغيير مخطط البيانات المحلي.
- استثناء نطاق MongoDB API من Service Worker حتى لا تُخزن استجابات البيانات الحية في Cache Storage.
- تحديث كاش التطبيق إلى `v40-mongodb-api-cache-first`.

## Revision 41 - تحسين النوافذ والسلاسة والكاش (120Hz-ready)
- إزالة النوافذ المنبثقة المغلقة من دورة layout/paint/compositing عبر `display:none` و`content-visibility:hidden` حتى وقت الفتح.
- تحويل حركة فتح النوافذ إلى `transform/opacity` قصيرة (110ms) ومناسبة للشاشات عالية التحديث بدون blur/backdrop-filter ثقيل.
- إلغاء حجز `will-change` للنوافذ المغلقة لتقليل استهلاك طبقات GPU والذاكرة.
- عزل رسم النوافذ المفتوحة باستخدام CSS containment لتقليل إعادة تخطيط بقية الصفحة.
- إصلاح MutationObserver في `cashtop-core.js`: معالجة الجزء الذي تغير فقط بدل إعادة فحص الصفحة كاملة عند إدخال صفوف/محتوى داخل النوافذ والجداول.
- جعل تحسين القوائم المنسدلة يعمل على subtree المتغير فقط بدل مسح كل عناصر select في الصفحة.
- Throttle لإعادة تموضع القوائم المنسدلة أثناء scroll/resize عبر requestAnimationFrame.
- تحديث Service Worker إلى `v41-120hz-modal-cache-first`.
- جميع صفحات HTML المحلية (34 صفحة) محفوظة ضمن App Shell وتُعاد من Cache Storage أولاً حتى عند توفر الإنترنت.
- تحديث الشبكة الصامت أصبح محدود التكرار: صفحات HTML كل 45 ثانية كحد أدنى، والملفات الثابتة كل 10 دقائق، لمنع منافسة الشبكة للواجهة أثناء الاستخدام.
- منع تكرار فحص App Shell وتسخين المكتبات الخارجية في كل صفحة ضمن نفس دورة Service Worker.

## Revision 33 — مزامنة متينة، تحديث المخزون، تقارير الفواتير والطباعة الحرارية

- إصلاح تعديل كمية مخزون المنتج بحيث تُحفظ الكمية الجديدة للمنتج والمتغيرات بدلاً من إعادة الكمية القديمة بعد الحفظ.
- تحويل رفع البيانات المعلقة إلى مزامنة مستقلة لكل مجموعة بيانات؛ تعثر مجموعة واحدة لا يوقف مزامنة المجموعات الجاهزة بعدها، وكل مجموعة تنجح تُزال فوراً من عداد العمليات المعلقة.
- إضافة backoff مستقل لكل مجموعة متعثرة مع إعادة المحاولة تلقائياً عند عودة الشبكة، مع إبقاء العمليات محلياً في LocalStorage ونسخة IndexedDB احتياطية وعدم مسحها عند تسجيل الخروج.
- منع إظهار رسالة الشبكة الخام `Failed to fetch` في المزامنة اليدوية واستبدالها برسالة عربية توضح أن البيانات محفوظة محلياً وستتم إعادة المحاولة.
- تسريع مزامنة الموظفين ومديري الفروع بعد الحفظ، بما يقلل تأخر ظهور الحسابات على الجهاز الآخر وحالات الحساب القديمة.
- إضافة تصفية سجل فواتير المبيعات: يومي، أسبوعي، شهري، سنوي، من تاريخ إلى تاريخ، أو جميع الفواتير.
- إضافة تصدير Excel وPDF احترافي لسجل الفواتير بحسب الفترة، مع اسم وشعار المحل، ملخص إجماليات، وتفاصيل السجل المعروضة دون محتويات أصناف كل فاتورة.
- إضافة حقل باركود مستقل لكل سطر في نافذة ترحيل عدة فواتير، مع تعبئة المنتج والسعر فور قراءة جهاز الباركود عند تركيز المؤشر في الحقل.
- الإبقاء على دعم قارئ الباركود الخارجي في الكاشير للإضافة الفورية إلى السلة، ودعم حقول الباركود المركّزة في المنتجات والمشتريات.
- إضافة محرك طباعة موحد `cashtop-printer.js` يستخدم تصميم الفاتورة الحرارية المرفق، مع مقاسات 58mm و80mm وA4، شعار الإعدادات، وإظهار/إخفاء باركود الفاتورة.
- إضافة خيار Bluetooth مغلق افتراضياً في إعدادات الطابعة؛ عند تشغيله يفتح ربط الطابعة ويحفظها للطباعة المباشرة عبر BLE، مع مسار Web Serial احتياطي لطابعات Bluetooth Classic/COM عند توفره.
- الطابعات المثبتة في نظام التشغيل تبقى مدعومة عبر نافذة الطباعة النظامية، مع مسارات Native Bridge لتطبيقات Android/iOS/Windows المخصصة.
- تحديث Service Worker لإضافة محرك الطباعة الجديد وتفعيل إصدار كاش جديد.

## Revision 49 — Mobile Modals + Cache First
- إصلاح حصر النوافذ المنبثقة داخل حاوية الصفحة على الجوال وإعادتها إلى منتصف نافذة العرض.
- إزالة التعتيم الأسود حول النوافذ على الجوال فقط.
- تمكين التمرير الكامل للنوافذ الطويلة حتى آخر المحتوى والأزرار.
- الإبقاء على تصميم اللابتوب والكاشير المكتبي دون تغيير.
- جعل فتح الصفحات يعتمد فوراً على الكاش حتى مع الإنترنت، مع منع تحديث الحزمة كاملة عند كل انتقال بين الصفحات.
