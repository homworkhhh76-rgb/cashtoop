# Cash Top R108 — Ghazal UI v3

هذه النسخة تحافظ على وظائف كاش توب R108، مع تحسين واجهة غزل والأداء على الجوال واللابتوب.

## تعديلات v3
- إعادة ترتيب سلة الكاشير كما في كاش توب الأصلي، بدون تغيير منطق البيع.
- إبقاء القائمة الجانبية الأصلية، وإغلاقها تلقائياً على الهاتف عند الضغط خارجها أو اختيار صفحة.
- إخفاء الشريط السفلي على الهاتف فقط.
- توحيد ستايل غزل على العملاء وباقي الصفحات: البطاقات، الجداول، النوافذ والحقول.
- نافذة المنتجات: مفاتيح إضافة منتجات متعددة والمقاسات بعرض كامل، وإصلاح زر الإغلاق.
- تصغير أيقونات بطاقات الإحصائيات لتكون بحجم متناسق مع لوحة التحكم.
- إظهار شعار الشركة في الهيدر/القائمة مع fallback للشعار الافتراضي عند فشل الرابط.
- Pagination تلقائية للسجلات الكبيرة: 18 صفاً على الهاتف و32 على اللابتوب/الكمبيوتر.
- IndexedDB يبقى طبقة التخزين المحلي المتينة، وطابور المزامنة لا يُحذف عند ضغط التخزين. تنظيف الكاش يتم بصمت.
- Service Worker Cache-First: الصفحات والملفات المحلية تفتح من الكاش فوراً حتى بوجود الإنترنت.
- صفحة تسجيل الدخول بقيت كما هي في Cash Top الأصلي.

---

# CASH TOP — Turso R74

هذه النسخة تعمل كتطبيق ويب ثابت على Turso بدون MongoDB أو Vercel أو Firebase Backend.

## الكاش وفتح الصفحات

- كل صفحات التطبيق المحلية محفوظة مسبقاً في **Cache Storage**.
- التنقل **Cache First** دائماً: الصفحة المحفوظة تُعرض فوراً حتى عند توفر الإنترنت، ثم يُحدَّث ملف الصفحة في الخلفية على فترات متباعدة.
- بيانات العمل المحلية تبقى متاحة فوراً من تخزين الشركة المحلي، ولا ينتظر فتح الصفحة تنزيل قاعدة البيانات.

## المزامنة الاقتصادية وشبه اللحظية

- إذا دخل المستخدم صفحة وهناك عمليات محلية غير متزامنة، يبدأ رفعها مباشرة في الخلفية.
- أثناء بقاء الصفحة ظاهرة يتم فحص **صف meta واحد فقط تقريباً كل 15 ثانية** لاكتشاف تعديلات جهاز آخر.
- يحتوي meta على طوابع تغيير لكل dataset؛ عند اكتشاف تغيير لا يتم تنزيل بيانات الشركة كلها، بل **الـ datasets المتغيرة والمطلوبة للصفحة الحالية فقط**.
- الصفحات المخفية لا تستمر في الفحص الدوري.
- الحفظ المحلي يظهر فوراً، والكتابات إلى Turso تبقى مجمعة/مؤجلة قليلاً لتقليل عدد عمليات الكتابة.
- لا يوجد Read-after-write للتحقق بعد كل حفظ، ولا Full Pull بعد كل تعديل.
- هذه مزامنة شبه لحظية اقتصادية وليست Push حقيقياً؛ لذلك يوجد فحص meta خفيف ما دام التطبيق مفتوحاً.

## الاستيراد

- يمكن استيراد نسخة كاملة أو نسخة قسم حتى لو كانت صادرة من **مفتاح شركة مختلف**.
- هوية المفتاح الحالي، الاشتراك والمدير الحالي لا يتم استبدالهم بهوية النسخة المستوردة.
- الأقسام المطابقة محلياً لا تُرفع مرة أخرى.
- الأقسام المتغيرة فقط تُرفع إلى Turso بدفعات UPSERT.
- لا توجد قراءة تحقق بعد كل كتابة ولا تنزيل كامل بعد الاستيراد.
- عند انقطاع الإنترنت تبقى التعديلات محلياً وتُرفع عند عودة الاتصال.

## الإشعارات

- تمت إعادة **قسم الإشعارات** وإشعارات المدير الذكية مثل المخزون المنخفض والاستحقاقات والتنبيهات التشغيلية.
- تم تعطيل **إشعار المدير الناتج عن إنشاء فاتورة مبيعات جديدة فقط**.

## تسجيل الدخول على الجوال واللابتوب

- بعد نجاح الدخول يتم تذكر **مفتاح الشركة فقط** بشكل دائم.
- بيانات جلسة المستخدم تبقى داخل التبويب؛ أضيف fallback عبر `window.name` لتجاوز مشاكل بعض متصفحات اللابتوب/أوضاع الخصوصية التي تعطل انتقال `sessionStorage` أثناء فتح لوحة التحكم.
- عند تسجيل الخروج يتم مسح جلسة التبويب، بينما يمكن إبقاء مفتاح الشركة لتسهيل الدخول القادم.

## الخطط

يوجد الآن: **Plus / Pro / مخصصة**.

الخطة المخصصة تسمح للمشرف العام بتحديد:

- حدود تتجدد يومياً: فواتير المبيعات، العملاء الجدد، المصاريف، الموردون الجدد.
- حدود ثابتة: الموظفون، المخازن، الفروع، المنتجات.
- ترك أي حد فارغاً يعني غير محدود.

صفحة الإعدادات تعرض مفتاح الشركة وتفاصيل الخطة فقط ضمن بطاقة الاشتراك.

## تنبيه أمني

هذه نسخة Static Web، لذلك Turso Auth Token الموجود في ملفات JavaScript يمكن قراءته من ملفات الموقع. بعد اكتمال الاختبار يجب تدوير التوكن الذي تم مشاركته في المحادثة واستخدام توكن جديد مناسب للتشغيل الفعلي.


## R106 — حماية السجلات والمزامنة الأوفلاين
راجع `R106-CHANGES.txt` لتفاصيل حماية المنتجات والفواتير، الاسترجاع من الأرشيف، شاشة المزامنة بعد الدخول، واستعادة المزامنة عند رجوع الإنترنت.


## تحديث الواجهة r2
- القائمة الجانبية عادت إلى تصميم Cash Top R108 الأصلي دون تعديل بصري.
- صفحة تسجيل الدخول عادت إلى التصميم الأصلي.
- تم حصر تصميم غزل في محتوى الصفحات والنوافذ والحقول والجداول والكروت.
- النوافذ والحقول تستخدم الآن أسلوب فاتورة مبيعات غزل: خلفية دافئة، حقول فاتحة، وعمودان متجاوران.
- تم تقوية إغلاق القائمة على الجوال عند الضغط خارجها أو اختيار رابط أو الضغط على Escape.

## تحديث R109 — 50 سجل لكل صفحة + تحسينات الواجهة
- جميع جداول السجلات تستخدم صفحات ثابتة: 50 سجل لكل صفحة.
- شريط الصفحات يظهر دائماً بالشكل: السابق — صفحة N — التالي، حتى مع 0 سجل.
- الجداول التي تستخدم محرك `renderVirtualRows` لا تُدخل إلا 50 صفاً في DOM في كل مرة لتقليل التعليق.
- الشريط السفلي يظهر على الجوال فقط ويختفي على اللابتوب والكمبيوتر.
- بطاقات الإحصائيات في المنتجات والعملاء والفواتير والمشتريات والمصاريف والموردين أصبحت بنفس النسب المدمجة لبطاقات الصفحة الرئيسية.
- واجهة الكاشير على اللابتوب أكثر ضغطاً: بطاقة البيانات أصغر، والسلة أوسع، وكل صنف في سطر مضغوط.
- شعار الشركة المرفوع من الجهاز يُضغط بصمت إلى قرابة 50KB، وبحد أقصى 100KB، ثم يُحفظ داخل إعدادات الشركة نفسها لتتم مزامنته مع قاعدة البيانات.
- عند ضغط التخزين المحلي، يتم تفريغ أقدم مرايا `localStorage` المتزامنة فقط بعد تأكيد نسختها في IndexedDB؛ العمليات المعلقة والبيانات المتينة لا تُحذف.
- Service Worker يعمل Cache-First وجرى رفع إصدار الكاش إلى R109.
